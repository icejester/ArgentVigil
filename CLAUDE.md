# ArgentVigil v1.23.0

Silver speculative-positioning monitor with gold as comparative context. Framing is **"selling dollars, not buying metals"** — is the speculative futures crowd genuinely capitulated, or just pulling back. Not a trading system: no price targets, no prediction framing, no risk-tolerance commentary. `SPEC.MD` is a retired holdover from an earlier one-big-file era (it once covered the Stock & Flow panel spec and CATCOR feature map together) — **do not reference it**; that content now lives inline in this doc's own Tab: Inventory and Tab: CATCOR sections below, following the same one-spec-file-per-feature convention as everything else. Companion spec docs (all gitignored, local-only, do not assume they exist in fresh clones): `deliveryBehavior-spec.md` (Delivery Behavior layer's full spec and story list), `dataHealth-spec.md` (Data Health/Fetch Status spec), `international-trade-spec.md` (Census international-trade ingestion spec), `fed-balance-spec.md` (Fed Balance Sheet Composition spec), `price-spec.md` (leverage history + LBMA fix spec), `catcor-events-spec.md` (Research Pane, Stage 1), `squeeze-context-spec.md` (COT/curve-spread pairing — Stories #1-3 implemented; see Tab: CoT's "Squeeze Context" subsection below). `frontend/docs/UI_STANDARDS.md` (checked in, not gitignored) covers cross-cutting interactive-UI conventions (legends, tooltips, color coding, sizing — check new interactive elements against it before inventing a new pattern); `README.md` covers the user-facing feature/data-source overview.

This doc is organized **by app tab** — the six sections below match `frontend/src/App.jsx`'s nav bar exactly (`SECTIONS`, in order: CoT [nav label **"Trading"**, `key: "cot"` — renamed from "CoT" at the user's request; this doc's `## Tab: CoT` heading and internal cross-references keep the "CoT" name since that's still the underlying data/module naming (`silver_cot_tracker.jsx`, `cot_silver`/`cot_gold` tables, `/api/cot/db`) — same doc-heading-vs-display-label split Money Supply's tab already has ("Money Supply" in nav, "Dollars and Sense" as the in-panel title)], Money Supply, Inventory, CATCOR, Research, Data). Each section covers that tab's frontend component(s), the backend routes/modules feeding it, its SQLite tables, and its known gaps/scope boundaries together, so you don't have to cross-reference three different doc sections to understand one panel end-to-end. Cross-cutting stuff that doesn't belong to one tab (repo layout, conventions, running the app) lives in its own sections at the end.

## Standing architectural rules (apply to every tab below)

- **Persist-on-fetch**: every route that talks to an upstream source has a `_fetch_and_persist_*` function that fetches, reshapes, and writes to SQLite; a paired `/db`-suffixed route reads the same data straight back out with no upstream call. **The frontend calls only `/db`-suffixed (or `/api/cot/db`, `/api/prices/db`) routes — it never triggers a live upstream fetch itself.** Rationale: CATCOR's and Delivery Behavior's cross-check features assume AV's database is already the real record, not a pass-through — a route that fetched live on every request would let two simultaneous reads observe two different upstream states, breaking that assumption. (Formerly documented in the retired `SPEC.MD`'s "Persist-on-Fetch Re-architecture" section — see the note on `SPEC.MD` above; this is now the canonical statement of the rule.)
- **One shared database**: `runtime/argentvigil.db` (gitignored), owned by `backend/db.py`, `DB_PATH` computed `__file__`-relative (two directories up from `backend/db.py`, into `runtime/`). Every tab's tables live here — there is no per-tab or per-layer database file. `runtime/` contains nothing else (an earlier flat-file cache for CATCOR's ForexFactory feed was migrated into a table — see CATCOR section below).
- **Backend package layout**: `backend/` is a real package (`backend/__init__.py`, empty, enables `from . import db` and `uvicorn backend.main:app`). `seed_data/` (manually-maintained static content) and `runtime/` (gitignored generated state) are namespace packages / non-packages sitting alongside it, not nested inside it.
- **Tiered background refresh** (`main.py`'s `lifespan`): two independent `asyncio` loops keep SQLite warm without the frontend ever calling upstream. **Fast tier** = spot prices only (the one genuinely intraday-moving figure). **Slow tier** = the other ~10 exchange-inventory sources (all move at most daily upstream). Both run once, unconditionally, at server startup, then default to **disabled** — each tier only repeats on its interval if explicitly turned on via `GET`/`POST /api/refresh/settings`. `POST /api/refresh/force` runs both tiers once immediately regardless of enabled state, returns real per-tier `{"succeeded","failed","errors"}` counts, and — only if at least one source actually succeeded — dispatches a `FORCE_REFRESH_EVENT` window `CustomEvent` that Inventory/CoT/CATCOR panels listen for to re-read their `/db` data immediately. CoT and Research are excluded from force-update (CoT stays pipeline-only; Research is on-demand chat, nothing to force-refresh).
- **Nulls over zeros**: metalcharts.org sometimes reports `0` to mean "not reported that day" — `_parse_aggregate_row` in `main.py` converts those to `None` so charts gap instead of showing false dips. Apply the same treatment to any new fields with this failure mode.
- **Stale spot feed over weekends/closures**: `/api/prices`'s underlying `twelvedata-ws` feed doesn't pause when COMEX/LBMA silver/gold trading is actually closed — confirmed live over a weekend that it kept returning a top-level `isStale: true` flag (sibling of `data`, not per-metal) alongside a `cacheAge` in the hours, while still stamping each entry with a live-looking `timestamp` and slowly drifting the price via rounding/re-sampling jitter on metalcharts.org's own end. `_fetch_and_persist_prices` (`main.py`) checks `isStale` **and** `date.today().weekday() >= 5` (real Sat/Sun) before skipping persistence — no new `spot_price_snapshot`/`spot_price_tick` row is written on a real weekend, so `PriceHistoryChart` goes flat at the last real print instead of showing fake weekend movement that never actually traded. On a weekday, `isStale=true` is persisted anyway rather than skipped — confirmed live that `isStale` can also fire on a weekday with `cacheAge` in the **months** (metalcharts.org's own upstream feed stuck, not a market closure); skipping indefinitely in that case would silently flatline the chart forever with no visible signal anything's wrong, so a stuck weekday feed is deliberately allowed to surface directly in the chart instead. Apply the same check to any other route that reads `/api/prices`' response.
- **The Data tab MUST be updated whenever app data changes — this is a strict requirement, not a nice-to-have.** As of datasources-spec.md's Story #1 split, this now spans two homes by kind of change: operational metadata (cadence, rate limit, `requires_env`, table ownership, affinity group) lives in **`backend/sources.py`** — any new/changed source's fetch cadence or rate-limit posture must land a matching `SourceDefinition`/`CadenceSpec`/`RateLimitSpec` edit there; editorial prose (per-field descriptions, curl examples, origin text) stays hand-written in **`frontend/src/data_editorial.js`** (`frontend/src/data_panel.jsx` renders both, joined by `sourceKeys`). Any change that adds/removes/renames a SQLite table or column, adds/removes/changes an upstream source or its fetch shape, or changes a source's fetch cadence/rate-limit posture must land a matching edit to the relevant one (or both) of these files in the same change. Treat a data-shape change without a Data-tab update as incomplete, the same as a migration without a schema update.
- **Pinned default tab**: a 📌 icon on each of `App.jsx`'s six nav buttons lets the user designate which tab opens by default on load, instead of always defaulting to CoT. Persisted server-side (`ui_settings` table, single-row upsert — same convention as `pipeline_runs` — via `GET`/`POST /api/ui/pinned-section`), **not** `localStorage` (the app has no client-only persistence anywhere; a shared setting like this belongs in the same SQLite database as everything else). `App.jsx` fetches the pinned section on mount and opens it if valid; falls back to `"cot"` if nothing's pinned or the fetch fails.

---

## Dev Notes (cross-spec, pre-feature findings)

Investigation results, environment quirks, and open threads that don't
belong to a shipped tab yet. Promote to a tab section (with a real spec)
once something here becomes a real feature; delete the note once promoted
or once superseded.

- **Yahoo Finance chart API query-shape sensitivity** (confirmed working
  as of 2026-07, see `squeeze-context-spec.md` Story #1): an earlier,
  isolated test of `interval=1d&range=5d` against a deferred-month
  contract symbol returned HTTP 429, raising a concern that this query
  shape or symbol type was specifically rate-gated. A follow-up
  investigation re-tested it directly — six back-to-back requests (no
  delay) against real deferred-month symbols (`SIU26.CMX`, `SIZ26.CMX`,
  `GCQ26.CMX`, `GCZ26.CMX`, plus `SI=F`/`GC=F` controls), using that exact
  query shape, all returned HTTP 200 with distinct real prices per
  contract month and a full year of daily history available. No 429s
  observed. The original 429 is now believed to have been a transient/
  IP-cooldown artifact from earlier rapid-fire testing, not a query-shape
  or symbol-type block — consistent with this endpoint's broader usage
  elsewhere in this codebase (`catcor.py`'s startup backfill already hits
  this same endpoint/shape against `SI=F`/`GC=F`, `main.py`'s
  `_fetch_yahoo_daily_closes` does too on-demand, with no documented 429
  anywhere, unlike ForexFactory's confirmed one — see `catcor.py`'s own
  rate-limit comment). This endpoint has no published quota and no
  rate-limit response headers (confirmed live) — any throttling posture
  has to stay conservative-by-design, not measured. `futures_curve_spread`
  ingestion (`main.py`'s `_fetch_and_persist_curve_spread`) uses a single
  retry with short backoff on 429/5xx as a defensive margin, not because a
  real recurring lockout has been observed.
- **COMEX contract-month liquidity does not match the textbook delivery
  cycle** (confirmed live, 2026-07, during the same investigation): Yahoo
  returns a price for every calendar-month futures symbol, including
  thin/illiquid ones. Silver's real near-term depth was concentrated in
  Sep (`SIU26.CMX`, vol=14,445) and Dec (`SIZ26.CMX`, vol=1,123) only,
  while Jul (`SIN26.CMX`, textbook "active" month) showed just vol=5; gold's
  real depth was Aug (`GCQ26.CMX`, vol=22,196) and Dec (`GCZ26.CMX`,
  vol=1,429), while Oct/Jul/Sep/Nov were all under 200. A hand-maintained
  "active delivery months" list was tried first for `futures_curve_spread`'s
  front/next-month resolution and abandoned once this was confirmed — see
  Tab: CoT's Squeeze Context subsection for the live-volume-ranking
  approach used instead. Relevant beyond this feature: any future work
  that needs "which COMEX contract month is currently liquid" should
  re-derive it live rather than trust a static list, including this note's
  own numbers, which are a snapshot, not a standing rule.

---

## Tab: CoT

**Frontend**: `frontend/src/silver_cot_tracker.jsx`. **Backend**: `pipeline/` (fetch + compute) + `backend/main.py`'s `/api/cot/db` route + `backend/delivery_behavior.py`'s category-composition compute (see below — this content is CoT-derived, not Stock & Flow, so it lives in this tab despite being part of the Delivery Behavior module).

### CoT: what it does

Fetches CFTC Commitment of Traders data for COMEX Silver (084691) and COMEX Gold (088691), computes a normalized positioning metric (`net_long_pct_oi` = (non-commercial longs − non-commercial shorts) ÷ total open interest × 100), and ranks it against rolling 2-year and 5-year historical windows. Normalizing by OI makes readings from 2011 and 2024 directly comparable.

**Signal zones** (configurable in `pipeline/config.py`): ≥90th percentile → specs crowded long (caution); ≤10th percentile → specs capitulated (back-up-the-truck zone); between → normal range, no signal. Both windows computed side-by-side; the UI flags disagreement explicitly.

### Panel contents (top to bottom)

The outer panel is titled **"Paper Games"** (renamed from "Positioning Extremes / Speculative Crowding" at the user's request — the App.jsx nav tab itself reads "Trading," a separate rename; this is the in-panel title, same distinction Money Supply already draws between its nav label and its own in-panel "Dollars and Sense" title). It is a **plain, non-collapsible header** (`<div className="comex-panel">`/`comex-panel-header`, not a `<details>`) — changed from collapsible specifically because a collapsible wrapper would let the panel-wide date selector (see below) get hidden along with everything it controls, the same reasoning that already drove Money Supply's own collapsible→plain-header fix. `StalenessLabel` still renders inline in the header.

- **Panel-wide date-range selector** (`PAPER_GAMES_WINDOWS`: 1M/6M/1Y/All + Custom, default 6M, lives in the "Paper Games" header) — drives every historical chart in the panel: `CombinedChart` (in its own nested collapsible "CoT Positioning & Gold/Silver Ratio" sub-panel, see below) and both Silver/Gold sections' `MetalLeverageCurveVolumeChart`. Deliberately **no 1W/2W presets** — COT positioning is weekly data, so a 1-2 week window would show at most 1-2 real points; a user who needs that granularity uses Custom instead (explicit user decision). **Custom** reveals From/To date inputs (same pattern as Money Supply's own Custom range) — client-side only, no fetch is re-triggered; `SilverCoTTracker` computes one shared `{since, until}` pair (`Date` objects or `null`) from either the active preset or the custom bounds and passes it down as props to all chart components, replacing what used to be each chart's own independent `days`-based `useState`. None of `/api/curve-spread/db`, `/api/*/db/leverage/history`, or `/api/cot/db` accept a window/date query param — every chart already fetches its full series once and filters client-side, so this restructure needed no backend changes.
- **Cross-chart pin (click-to-highlight)** — the same mechanism Money Supply's own tab already established (see Tab: Money Supply below for the fuller precedent), rebuilt here for the Paper Games panel: `pinnedDate` (a shared string) lives in `SilverCoTTracker`, threaded down as `pinnedDate`/`onPin` props to `CombinedChart` and both metals' `MetalLeverageCurveVolumeChart` — **any chart in the panel can originate a pin** (click a point sets it), not just one designated "picker" chart, since none of these charts have a competing click/drag gesture the way Money Supply's purchasing-power chart does (its own mousedown/mouseup are claimed by a press-and-hold comparison gesture, which is why that one chart can only display a pin, never originate one — no such conflict exists in this panel). Cleared via the 📌 button in the "Paper Games" header (`📌 {pinnedDate}`, click clears) — not right-click, same reasoning as Money Supply (Safari intercepts `contextmenu` unreliably).
  - **Per-chart nearest-date snapping**: `nearestRowDate(rows, pinnedDate)` (module-level helper, direct copy of `money_supply.jsx`'s own function of the same name/behavior) finds the nearest real row on/before the pinned date, falling back to the nearest row after it if the pin predates a given chart's own data. Necessary because `CombinedChart` (CoT weekly Tuesdays) and `MetalLeverageCurveVolumeChart` (curve spread's own daily dates) have genuinely different date grids that rarely share exact date strings — Recharts' category-axis `ReferenceLine` only renders when its `x=` exists verbatim in that chart's own dataset, so each chart snaps the shared `pinnedDate` independently before drawing its own line.
  - **One tooltip-content function, two triggers** (per `UI_STANDARDS.md`'s Tooltips section) — both chart types had their hover tooltip refactored out of an inline Recharts `formatter=`/`contentStyle=` into a standalone function component (`CombinedChartTooltipContent`, `MetalLeverageCurveVolumeTooltipContent`), each taking `{ active, label, ...data }` and looked up by `label` from that chart's own already-merged array — same shape as `money_supply.jsx`'s `MoneySupplyTooltip`/`CompositionTooltipContent`/`QeQtTooltipContent`. The live Recharts `<Tooltip content={<Component .../>}>` (hover) and a fixed box rendered below the chart when a pin is set (`<Component active label={pinnedDateSnapped} .../>`) both call the exact same component — one source of truth, not duplicated markup for the two trigger paths. `MetalLeverageCurveVolumeChart` (metal-parameterized, see below) reuses the same tooltip/pin plumbing for both Silver and Gold rather than each metal owning its own copy.
  - **Legend-click (`clickedKey`, series highlight) and chart-click (pin) coexist safely** — confirmed by construction, not just assumed: the legend rows are DOM siblings of `<ResponsiveContainer>`, not children of the Recharts SVG tree, so a legend-button click cannot trigger the chart's own `onClick={(state) => state?.activeLabel && onPin(...)}` handler. Money Supply already runs this exact combination (3 charts, each with both a legend-click-highlight state and a chart-click-to-pin handler) with no documented collision — same precedent relied on here rather than re-verifying from scratch.
  - `MetalCurrentReadout` (the compact header current-value readout, both metals) is **deliberately NOT pin-aware** — it always shows the true latest reading regardless of any pinned date, same as Money Supply's stale-data warning being suppressed while pinned (a header/summary staying "live" while charts below it show a historical pinned moment is the established pattern, confirmed with the user rather than assumed).
- **CoT Positioning & Gold/Silver Ratio** (`CombinedChart`, wrapped in its own nested collapsible `<details>`, **defaults open**, directly under the "Paper Games" header) — silver + gold net long % of OI, driven by the panel-wide selector above (no independent window buttons of its own). `StalenessLabel` ("CoT data as of ... · published ~...") moved into this sub-panel's own `<summary>` (out of the "Paper Games" panel header it used to sit in) — it's specifically about *this* chart's data freshness, not the whole "Paper Games" panel, so it belongs on the sub-panel whose data it describes. No date-range text in the title anymore (previously showed the plotted series' real start/end dates; removed at the user's request — the panel-wide selector above already communicates the active window). When only one metal is visible, 10th/90th percentile reference lines appear.
  - **Legend brought up to `UI_STANDARDS.md`**: `COMBINED_CHART_LEGEND` (same `key`/`legendLabel`/`color`/`eli5` shape as Money Supply's `M2_LEGEND_SERIES`/`COMPOSITION_SERIES`/`QE_QT_LEGEND_SERIES`), horizontal layout (`comex-legend-list--horizontal`). Clicking a row highlights that series (`clickedKey` state — thicker stroke + full opacity on the clicked line, `strokeOpacity: 0.3` dimming on the others, mirroring `money_supply.jsx`'s `clickedM2Key`/`clickedPieKey` pattern) and opens a collapsed detail panel below the legend showing that series' `eli5` text (previously always-visible inline prose next to each swatch — now click-revealed, consistent with every other legend in the app). The active row also gets `legend-btn-row--baseline`'s swatch outline, same "this one's selected" visual as Money Supply's own legends use (a generically-named class from that original use case, repurposed here — not baseline-specific in meaning).
  - **A real bug the user caught and had fixed**: an earlier version had clicking a row ALSO hide/show that line entirely (`hidden` state, `<Line hide={...}>`), on top of the new highlight-on-click behavior — two different click semantics fighting each other (click once to highlight, click again and the line vanishes instead of un-highlighting). Fixed by removing the disappearing-line behavior outright — `hidden` state, the `hide` prop, and the `legend-btn--off` dimmed-when-hidden styling are all gone; a legend click now does exactly one job (highlight + dim others + open the eli5 detail panel), never removes a line from the chart. The percentile reference lines' "only show for whichever metal is alone" condition (`silverAlone`/`goldAlone`) was redefined to key off `clickedKey` (a metal is "alone" when it's the one currently clicked/highlighted) instead of the old "the other metal is hidden" condition, since hiding no longer exists.
  - **`eli5` text brought up to Money Supply's actual voice** (per the user's explicit request to match "detail and snark") — the split Money Supply itself uses: **hover tooltips stay purely factual** (this chart has no custom hover tooltip at all, same as every Money Supply chart — Recharts' bare default), and the **editorial voice lives specifically in the click-revealed eli5 text**, matching the real precedent (`WRESBAL`/`RRPONTSYD`/etc.'s "who benefits," dry framing about who's actually getting paid and why) rather than inventing a different split. Silver's `eli5` covers positioning-extremity mechanics and why it matters more here than in a market where physical backing actually matches paper claims (cross-references Paper Leverage's ~5-6x OI/registered gap); Gold's frames it as comparative context per AV's standing "gold as context, not a second thing to trade" rule; GSR's explains the inverted-axis convention and historical range. All three end with an explicit non-prediction disclaimer, consistent with AV Voice Rules (no "alarming"/"critical" framing, no prediction framing on positioning/track-record figures).
- **Gold/Silver Ratio** — inverted right axis (lower GSR = silver outperforming = line goes up), computed from COMEX futures spot (GC=F ÷ SI=F), not ETF prices.
- **Who's Holding Long Positions** (`CategoryCompositionPanel`, collapsible, **defaults open**) — disaggregated CoT category share-of-long-OI over time, both metals, own metal toggle, own fetch of `/api/delivery-behavior/db`. Backed by `compute_category_composition` in `backend/delivery_behavior.py` (see "Category composition" below).
- **Silver / Gold sections** (collapsible, **default closed** — a deliberate flip from earlier open-by-default). `SignalTrackRecord` (the historical +4wk/+8wk hit-rate description panel, along with its `ZoneTable`/`HitRateRow` helpers) was **removed entirely** at the user's request, not just hidden — the feature had run its course. **Silver was rebuilt from scratch first** (2026-07, the user's explicit "scrap everything under Silver, rebuild from a short list" request) into a deliberately smaller set of components; **Gold was brought to full parity in a later pass** ("what we did to silver, we need to do to gold — same leverage/curve/volume chart, same moving of signal to surface on legend click, everything") by generalizing Silver's rebuilt components to take a `metal` prop rather than duplicating them, per this codebase's established "generalize, don't duplicate" convention. Both metals now share one component set — there is no longer a Silver-specific vs. Gold-specific code path anywhere in this section.
  - **`MetalLeverageCurveVolumeChart({ metal, since, until, pinnedDate, onPin })`** (generalized from a Silver-only `SilverLeverageCurveVolumeChart`) — one combined chart per metal: paper leverage as a line (yellow/amber, left axis), curve spread as a line (blue, right axis), volume as gray bars on a third, unlabeled/auto-scaled axis purely for visual scale — a Recharts `ComposedChart`, not `LineChart`, since `Bar`+`Line` together need it. This is the ONLY chart in either metal's own `<details>` section. Merges on curve spread's own dates (its ~290-row daily series is the densest of the three), nearest-date-matching leverage (±3 days, same pattern the old standalone `LeverageCurveSpreadChart` established before it was superseded) and volume (±1 day) onto it. **Volume will show as sparse recent bars, not a full-window series** — `db.get_volume_series(metal)` only has real history from whenever this feature started polling (11 days as of the initial Silver rebuild) — an honest reflection of how little real volume history exists, not a bug to hide or backfill around. `GET /api/volume/db/history?metal=` is the read route (`main.py`). Reads `{ label, leverageHistoryUrl, spotKey } = METAL_CONFIG[metal]` and `metalChartLegend(label)` (below) rather than hardcoding either metal's identifiers.
    - **Recharts hidden-axis rendering bug, found and fixed during the initial Silver build**: the volume bars' hidden scale axis (`<YAxis yAxisId="volume" hide>`) originally used `domain={["auto","auto"]}` against a series that's null on ~95% of rows (sparse real volume history) — Recharts computed a degenerate/invisible scale against that mostly-null domain, so the bars silently failed to render even though the data was correct. Fixed with `domain={[0, "auto"]}` + the `includeHidden` prop (confirmed real in this repo's installed Recharts 3.9.0 via its own TypeScript defs, not guessed) + an explicit `barSize={4}` + `isAnimationActive={false}` on the `<Bar>` itself.
  - **`MetalCurrentReadout({ metal, compact = false })`** (generalized from a Silver-only `SilverCurrentReadout`) — paper leverage value + open interest in contracts/oz + volume + as-of date, the CFTC-derived current-value block. `compact=true` renders one inline `<span className="staleness-label">` (`6.52x · 124,065 contracts (620,325,000 oz) · Vol 37,294 · as of 2026-07-14`, same right-aligned `margin-left: auto` convention `StalenessLabel` already established for a `<summary>` row) — used in both the Silver and Gold `<details>` summaries. The full-card form (default, `compact` omitted) is kept for any other call site that wants the bigger look, though nothing currently uses it.
  - **Positioning percentile/classification (`SignalDetail`) is metal-generic and surfaces only inside `CombinedChart`'s legend click-reveal** — not its own always-visible panel. `CombinedChart` takes `silverLatest`/`silverWindows`/`goldLatest`/`goldWindows` props; clicking "Silver" or "Gold" in the combined chart's legend (`clickedKey === "silver"`/`"gold"`) renders `<SignalDetail latest={...} windows={...} />` below that row's `eli5` text, per the user's explicit "surface this when clicking the metric in the legend, hover the chart for a quick look" framing. `SignalDetail` shows, on one center-justified line: the raw net-long % of OI (colored red/green/gray by combining both windows' classifications — gray only if both windows read normal, red if either is crowded, green if either is capitulated and neither is crowded), then the 2-year and 5-year percentiles (each independently colored red/capitulated-green/gray via `signalDetailColor()`), separated by `·`. Label text ("Net Long % of Open Interest:", "2-Year:", "5-Year:") is `#8a94a6` (this file's tooltip gray); only the data values carry classification color. `SignalBanner` (the older, bulkier two-window-card version this replaced) and `signalColor()` (its dedicated color helper) are **both fully deleted** — confirmed via grep, zero remaining references — now that Gold no longer needs them either.
  - **Legend brought up to `UI_STANDARDS.md`**: `metalChartLegend(metalLabel)` (generalized from a Silver-only `SILVER_CHART_LEGEND` constant into a function, since Gold needed the identical mechanism with only the label text substituted — the `eli5` prose is already metal-generic) returns the same 3-entry `{key, legendLabel, color, eli5}` shape as `COMBINED_CHART_LEGEND`/Money Supply's own legends. Clicking a row highlights that series (thicker/full-opacity line, or fuller bar opacity for volume) and dims the other two, and opens a collapsed `eli5` detail panel below the legend. `eli5` text: leverage explains the ratio's real meaning and its "whole market, not front-month-scoped" caveat; curve spread cross-references the Squeeze Context section's two real front/next-resolution bugs; volume is explicit about having no CFTC equivalent and no backfill path.
  - **"Current" numeric readouts are deliberately insulated from the shared selector** — `MetalCurrentReadout`'s leverage value/OI/volume/date block (from `/api/silver|gold/db/leverage`, the current-snapshot route, not `/history`) and `SignalDetail`'s percentile/classification are window-independent (separate routes/derivations from any historical series), regardless of which window the shared "Paper Games" selector or a cross-chart pin currently has active.
  - **Everything from the older, richer component set — `PaperLeveragePanel`, `CurveSpreadPanel`, `LeverageCurveSpreadChart`, `SignalBanner`, `LbmaFixBadge`, `LeverageSpotBadge`, `PriceHistoryChart`, `spotPrice()`, `signalColor()`, and their supporting constants (`PRICE_HISTORY_WINDOWS`, `PRICE_LIVE_POLL_MS`, `DATE_ONLY_TICKS_ABOVE_HOURS`) — is now fully deleted**, not just unreachable for one metal. This removed the LBMA fix badge, the live spot-price badge, the rolling spot-price chart with its "Live" polling toggle, and curve spread's own separate current-value badge+chart from both metals' sections entirely — those pieces of information (LBMA settlement reference, live spot quote, rolling price history) are no longer shown anywhere in the Silver/Gold sections. `LbmaFixBadge`'s backing routes (`GET /api/lbma/db`, `GET /api/lbma/db/history`) and `PriceHistoryChart`'s backing routes (`/api/prices/db`, `/api/prices/db/ticks`) are unaffected — this was a frontend-only removal, the backend/DB machinery described below is still live and still populated, just currently has no frontend consumer in this tab.
  - Leverage itself is `open interest × contract size ÷ registered inventory`, 5,000oz silver contracts / 100oz gold.
  - **Leverage is CFTC-only, everywhere it's shown** (`price-spec.md` Section 1 describes an earlier, superseded design — see the correction below) — `db._leverage_backfill_from_cot`/`db.get_leverage_history(metal)` (`GET /api/silver|gold/db/leverage/history`) and `db.get_latest_leverage(metal)` (`GET /api/silver|gold/db/leverage`, the current-value badge) both compute purely from `cot_{silver,gold}.open_interest` (CFTC's own `open_interest_all` field, real weekly history back to 2011) joined against `{inventory,gold_inventory}_aggregate.registered` as-of each CoT report date (nearest date on/before, same convention `VaultSnapshotPanel` uses for pinned-date lookups). One source, weekly resolution throughout, no daily tail spliced on.
    - **A real bug found and fixed 2026-07**: the original design (still what `price-spec.md` describes) stitched this CFTC-derived weekly backfill together with `volume_oi`/`gold_volume_oi`'s own daily `openInterest` field (sourced from metalcharts.org's `volume-oi` endpoint) — the backfill covered older dates, the daily metalcharts.org rows covered recent ones, spliced into one series on the assumption the two OI numbers were "a valid substitute for each other." A user-driven investigation confirmed live that assumption was wrong: on every date checked where both sources existed for the same contract, metalcharts.org's `openInterest` ran a stable **~15% below** CFTC's real `open_interest_all` (e.g. 2026-06-30: metalcharts.org 108,964 contracts vs. CFTC's real 127,000) — not noise or a rounding difference, a consistent gap, most plausibly metalcharts.org excluding some category CFTC's total includes (spread positions, non-reportable small traders — unconfirmed which one). Splicing two non-equivalent OI numbers together produced a real, visible cliff in the leverage chart right at the backfill/daily-data seam: confirmed live, silver's leverage stepped from 7.78 (last CFTC-backed week) to 5.86 (first metalcharts.org-backed day) even though neither underlying input had actually moved anywhere near that much — a data-source bug wearing a chart-rendering-bug costume, not a math error. Fixed by dropping metalcharts.org's OI from the leverage calculation entirely; confirmed live post-fix that the same week-over-week transition is now a smooth 7.78 → 6.86 → 6.62 → 6.52, no cliff.
    - **Real cost of the fix**: the current-value leverage badge is now only as fresh as the latest CoT report (weekly), not daily — a real, accepted staleness tradeoff, matching how `SignalDetail`'s own percentile/classification readings already work. `volume` stays a separate, genuinely-daily figure (CFTC's CoT report has no volume field at all) — `get_latest_leverage` looks it up independently from `volume_oi`/`gold_volume_oi`'s own latest row, so the badge's `volume` reading can legitimately be a day or two newer than its `openInterest`/`paper_leverage` reading; that's expected, not a bug.
    - **`volume_oi`/`gold_volume_oi` still exist and are still fetched every slow-tier cycle, but only for `volume` now** — `_fetch_and_persist_silver_leverage`/`_fetch_and_persist_gold_leverage` (`main.py`) stopped writing `open_interest`/`paper_leverage` into these tables (both persisted as `NULL` going forward) rather than leaving a second, confirmed-~15%-off OI number sitting in the database where a future investigation could mistake it for something usable. `db.get_volume_oi_series`/`get_gold_volume_oi_series` (the old daily-series readers, no longer called by anything) were deleted outright rather than left as dead code.
    - **Real backfill ceiling is set by `registered` itself, not by CoT's 2011 coverage or `total`'s 1992 coverage**: metalcharts.org's registered/eligible split is NULL before **2020-01-02 for silver** and **2026-02-17 for gold** (confirmed live — an upstream reporting-format gap, not a fetch bug), so silver backfills to 2020-01-02 and gold's backfill contributes almost nothing today. This ceiling is unchanged by the CFTC-only fix — it was never about which OI source was used, it's the registered-inventory denominator's own upstream coverage.
  - **LBMA fix** (`price-spec.md` Section 2): `lbma_fix` table + `_fetch_and_persist_lbma_fix()` (`backend/main.py`) sources gold's AM fix and silver's daily fix from GoldAPI.io's free tier (`GAPI_API_KEY` env var, 500 req/month), via `GET https://www.goldapi.io/api/{XAU|XAG}/USD/{YYYYMMDD}` — the **date-suffixed historical endpoint specifically**; GoldAPI.io's bare `/api/{SYMBOL}/USD` current-price endpoint is a FOREXCOM spot feed (confirmed live), not LBMA, and is never called. Confirmed live against a real key that GoldAPI.io has **no data for "today"** until some lag later in the day — `_fetch_and_persist_lbma_fix` tries today's date first, falls back to the most recent real business day if empty, so the badge/history never goes blank mid-day. Also confirmed live that GoldAPI.io stamps both metals' historical rows with an identical `10:30:00Z` timestamp regardless of metal — real LBMA fix times are gold AM 10:30 UTC / PM 15:00 UTC, silver 12:00 UTC (winter), so silver's stamp does **not** match its real fix time; the `date` field is treated as "which calendar day this fix is for," not a verified fix-moment timestamp. GoldAPI.io also exposes only one price/day for gold — no AM/PM-distinct field — so **gold's PM fix is not available from this source**; persisted as `fix_type="AM"` (best-effort) for gold, `fix_type="daily"` for silver. `price_usd` has not been independently cross-checked against LBMA's own official print (that requires a paid IBA license — see `price-spec.md` Section 2). Cadence: **startup-only + manual force-refresh** (`lbma_fix`'s `CadenceSpec.trigger` is `"startup"` in `backend/sources.py`, deliberately excluded from `_schedule_loop`'s recurring dispatch and from `POST /api/refresh/force`'s fast/slow tiers, since a 1-2x/day source doesn't fit either tier's daily-or-faster assumption) — reachable via the Data tab's per-source "Re-run now" button. `GET /api/lbma/db?metal=` / `GET /api/lbma/db/history?metal=&fix_type=` are the paired read routes. **No frontend consumer as of the CoT tab's component-set consolidation** — `LbmaFixBadge` (the sole UI reader of these routes) was deleted along with the rest of the old `PaperLeveragePanel` component set (see the Silver/Gold sections bullet above); the table/routes/fetch-and-persist machinery are all still live and still populated, this data just isn't currently surfaced anywhere in the app.
    - **A real bug caught 2026-07** (during the Squeeze Context curve-spread investigation, prompted by "the curve also looks wrong" applying scrutiny to LBMA fix too): GoldAPI.io does **not** error for a weekend date the way it does for "today, not yet published" — confirmed live, it silently returns the prior Friday's real fix under a non-null `price` field, re-timestamped as if it were Saturday's/Sunday's own fix (no LBMA fix is ever actually set on a weekend). The original fallback only retried on `price is None`, which a weekend response never triggers — so a fetch that happened to run on a Saturday/Sunday would have silently persisted Friday's real number mislabeled with a weekend's date. No contaminated rows were found in `lbma_fix` by the time this was caught (pure luck of when the backend happened to restart), but the bug was real and latent. Fixed by walking the fetch date back to the nearest real weekday **before** ever calling GoldAPI, rather than trying to detect the silent carry-forward after the fact (unreliable — two consecutive real prices could coincidentally match, so that inference doesn't distinguish a real repeat from a forward-filled one; the weekday check is unambiguous).
    - **Investigated and ruled out**: whether Yahoo Finance (the source now powering `futures_curve_spread`, see below) could also supply the LBMA fix, to consolidate onto one upstream. Confirmed it cannot — the LBMA fix is an auction-determined benchmark price administered by ICE Benchmark Administration, a fundamentally different kind of instrument from anything Yahoo's chart API serves (continuously-traded futures/spot/ETF quotes only). Yahoo's own symbol search surfaces only third-party ETFs/indices that *track* the LBMA Gold Price (e.g. Tel Aviv-listed `KSM-F6.TA`), not the fix itself, and only for gold PM specifically — not a real substitute. GoldAPI.io (or an equivalent LBMA-licensed/tracking source) remains the only viable path for this data; there is no free-source consolidation opportunity here.
    - **This month's GoldAPI.io free-tier quota (500 req/month) was exhausted 2026-07** by direct manual testing during the above investigation (confirmed live: `403 Monthly API quota exceeded. Upgrade to Unlimited reqs/month plan.`) — not by the app's own normal ~2 req/day cadence, which stays far under cap on its own. `lbma_fix` will not receive new real data until the quota resets on GoldAPI.io's next billing cycle (or the plan is upgraded); the weekend-fix bugfix above is confirmed correct via offline date-logic testing only, not yet re-verified against a live fetch.
- **Staleness label** (inline in the "Paper Games" panel header, alongside the date-range selector) — "CoT data as of ... · published ~..." only; no longer takes a `generatedAt` prop or renders a "pipeline run" clause (that's tracked backend-side via `pipeline_runs`/`get_last_run_at()` but no longer surfaced here).

No "Macro Watchlist" section exists anymore (removed; `/api/cot/db` drops `macro_watchlist` entirely).

### CoT: data flow

`pipeline/run.py` → parses CFTC rows (via `pipeline/fetch.py`, stdlib-only Socrata client) → persists append-only to `cot_silver`/`cot_gold` (`INSERT OR IGNORE` keyed by `report_date` — a published report is never overwritten) + upserts `cot_prices` (ticker+date, weekly, via `pipeline/price_fetch.py`, Yahoo Finance) → stamps `pipeline_runs` (single-row, `id=1`) with completion time, unconditionally, even if no new CoT rows landed → also writes `pipeline/cache/cot_data.json` unchanged (standalone-CLI usability only; frontend never reads it). Persistence goes through `from backend import db` (shared `backend/db.py`, itself pure `sqlite3`/`os`/`contextlib`, no `fastapi`/`httpx`, so importing it doesn't need the venv) — `pipeline/` no longer owns a separate database file.

`GET /api/cot/db` reads `cot_silver`/`cot_gold`/`cot_prices` directly via `db.get_silver_series`/`get_gold_series`/`get_price_series` and recomputes percentiles/track-record/GSR server-side on each request (cheap, ~1,560 rows/metal) using `pipeline/compute.py`'s `compute_from_series`/`compute_signal_track_record` (`compute_from_series` is an additive twin of `parse_and_compute` that takes already-normalized SQLite rows instead of raw CFTC rows). **`/api/cot/db` never triggers a live Socrata fetch** — CoT refresh stays external (`python3 pipeline/run.py`, manual or cron), a deliberate asymmetry from every other tab's `/refresh`+`/db` pattern, since a 15-year Socrata pull is too slow to trigger from an HTTP request. `POST /api/refresh/force` does not touch CoT or `pipeline_runs` either.

As of the Data Health work (see Data tab below), `cot_pipeline` also has its own registry entry (`_refresh_cot_pipeline` in `main.py`) — a rate-limit gate (skips, `last_attempt_status="skipped"`, if the latest persisted report is under 7 days old) wrapping `asyncio.to_thread(pipeline_run.run_pipeline_once)`. This is the first in-app trigger CoT has ever had, reachable via the Data tab's per-source "Re-run now" button; it does not change the fact that `/api/cot/db` itself stays read-only.

### Category composition (disaggregated CoT — powers "Who's Holding Long Positions")

`compute_category_composition` in `backend/delivery_behavior.py`: shares of long OI by CFTC Disaggregated category (`producer_merchant`/`swap_dealer`/`managed_money`/`other_reportable`) per `report_date`, both metals, reading the `cot_disaggregated` table (fetched by `pipeline/run.py` via `pipeline/fetch.py`'s `fetch_disaggregated_cot_data`/`fetch_gold_disaggregated_cot_data`, CFTC's Disaggregated Futures-Only Socrata dataset `72hh-3qpy` — a different dataset ID from Legacy's `jun7-fc8e`; both silver `084691`/gold `088691` codes work against it, same codes as Legacy). `producer_merchant` has no spread field in CFTC's schema, so its `spreading` is always `None` by design, not a data gap. Requires all 4 categories present for a `report_date` before computing shares (skips the week otherwise) rather than silently normalizing over an incomplete total — a code-review finding caught before shipping (an earlier version always sourced `open_interest` from `producer_merchant`'s row specifically and could understate `total_long` if any single category's `INSERT OR IGNORE` failed to land).

Also carries an approximate `days_to_fnd_approx` per week from Delivery Behavior's First Notice Day rule engine (see Inventory tab below) — explicitly documented as a directional marker only, not a claim about which contract month any category's OI actually sits in (the Disaggregated report aggregates across every open contract month; AV has no contract-month-level OI to disambiguate — see "Permanently out of scope" under Inventory).

### Squeeze Context (`squeeze-context-spec.md`)

Pairs COT positioning extremity with a futures curve spread, plus a small hand-maintained historical case log — "was the specs crowd genuinely stretched, or is this just the front-vs-next-month curve shape," each series carrying its own explicit as-of date, never implied simultaneity. Not a composite score, not an alerting system, not a new tab. Grew out of a conversation about the January 2026 silver spike (>$120/oz), where curve backwardation and stretched Managed Money positioning both preceded the move.

- **Futures curve spread** (`futures_curve_spread` table, `main.py`'s `_fetch_and_persist_curve_spread`, slow tier, `GET /api/curve-spread/db?metal=`) — front-month vs. next-month COMEX settlement spread, both metals, daily. `curve_spread_pct = (next_month_price - front_month_price) / front_month_price`; positive = contango, negative = backwardation; NULL (not 0) on any day either leg has no real price. Sourced from Yahoo Finance's chart API against real deferred-month contract symbols (e.g. `SIU26.CMX`) — see the Dev Notes section above for the rate-limit and contract-liquidity investigation that unblocked this.
  - **Front/next-month resolution is liquidity-ranked PER HISTORICAL DATE, not once for "today" and backfilled** — this went through two real bugs, both caught by inspecting the persisted data after initial ship rather than trusting the mechanism on paper. **Bug 1**: the first version ranked liquidity once for today's date, then fetched each of those two symbols' entire trailing-year history and stored it all under that single label. Since which contract is genuinely front/next changes over time (confirmed live: during the real January 2026 squeeze, the actually-liquid front month was `SIN26.CMX`/Jul26, not `SIU26.CMX`/Sep26 — what a "rank once for today" approach would have wrongly applied retroactively), this produced a spread series that looked suspiciously flat (0.71%-1.13% for the entire year, barely reacting to a real >80% spot move) — not because the underlying Yahoo prices were fake (they're real, independently-traded, with real daily volume — confirmed via each symbol's `longName`/distinct 52-week-high-low/distinct daily volume, e.g. `SIU26.CMX` had genuine 14,445-volume days and `SIN26.CMX` had genuine 2,800-9,700-volume days during the squeeze itself), but because the LABEL was wrong for most of the backfill window. Fixed by fetching each candidate symbol's full daily (close, volume) history once (`_fetch_yahoo_contract_daily` now returns per-day volume from the response's own `indicators.quote[0].volume` array, not just `meta.regularMarketVolume`'s today-only snapshot), then re-ranking front/next independently for every date using that date's own real volume. **Bug 2**, caught immediately after fixing Bug 1: pure per-date volume ranking (highest volume = front, second-highest = next) could pair an about-to-expire contract's trailing volume against a genuinely newer front month and call the chronologically EARLIER contract "next" — e.g. early Dec 2025 briefly ranked `SIZ25.CMX`/Dec25 (winding down, still had residual volume) as second-highest against `SIN26.CMX`/Jul26 (the real front month by then), producing a spurious ~-2.7% "negative spread" that was a backwards pairing, not real backwardation. Fixed with `_delivery_sort_key` (parses year/month from the symbol) — "next" must be the highest-volume REMAINING candidate whose delivery month is strictly later than front's, not just the second-highest by volume regardless of month order. **After both fixes, real data across the full trailing year (2025-05 through today) never goes negative** — range is roughly +0.6% to +4.6%, tightest during the January 2026 squeeze itself (~0.6-0.7%, `SIN26.CMX`/`SIU26.CMX`) and widest for the widest month-gap pair encountered (`SIZ25.CMX`/`SIZ26.CMX`, a full year apart, naturally carries more cost-of-carry). This is treated as a real finding, not a remaining bug — the front/next spread specifically didn't show backwardation for this particular event; that may mean physical stress concentrated more at spot-vs-front-month than between two already-deferred paper contracts (see the spot-vs-front-month series below, which is forward-looking-only and can't retroactively confirm this).
  - `_candidate_contract_symbols` sweeps both directions from today (`CURVE_SPREAD_CANDIDATE_MONTHS_AHEAD`=8, `CURVE_SPREAD_CANDIDATE_MONTHS_BEHIND`=14) — the "behind" sweep is what Bug 1's fix needed: enough trailing candidate months that a per-date backfill ranking has every symbol that could have genuinely been front/next at any date in the ~370-day fetch window, not just symbols visible from today's vantage point.
  - A hand-maintained "active delivery months" list (e.g. the textbook silver Mar/May/Jul/Sep/Dec cycle) was tried first for month *selection* and abandoned even before the two bugs above, once live testing showed COMEX's real near-term liquidity doesn't match that textbook cycle (see Dev Notes) — mirrors the same reasoning `delivery_behavior.py`'s own module docstring gives for computing FND/LTD per-month on demand rather than off a small fixed list.
  - Single retry with short backoff on 429/5xx per request, then skip-and-log without failing the rest of the slow tier.
- **Display**: originally a standalone `CurveSpreadPanel` component (`silver_cot_tracker.jsx`) — a current-value badge (front/next symbols, spread %, contango/backwardation label, its own as-of date, always window-independent) plus its own history chart. **Deleted along with the rest of the old component set** during the Silver rebuild/Gold parity work (see the Silver/Gold sections bullet above under Panel contents) — the current-value badge is gone entirely (no frontend surface for front/next symbols or the contango/backwardation label as of this consolidation), and the history chart's curve-spread line lives on inside `MetalLeverageCurveVolumeChart` instead (blue line, right axis, `curve_spread_pct`), sharing that chart's `since`/`until` props from the "Paper Games" panel-wide selector rather than owning independent window state. `GET /api/curve-spread/db?metal=` is unaffected — still the read route `MetalLeverageCurveVolumeChart` fetches from. UI copy standardizes on "curve spread"/"front-month spread," never "the curve" alone (spec's terminology rule — "the curve" is reserved for a true multi-month curve-shape visualization, not built in this pass).
- **Paper leverage vs. curve spread combined chart** — originally shipped as a standalone `LeverageCurveSpreadChart` component (dual-axis line chart, `paper_leverage` left axis vs. `curve_spread_pct` right axis) sitting below `CurveSpreadPanel` in each metal's section. Both were superseded by `MetalLeverageCurveVolumeChart` (see the Silver/Gold sections bullet above under Panel contents) during the Silver rebuild/Gold parity work — the combined chart's pairing logic described below is still exactly how `MetalLeverageCurveVolumeChart` merges these two series today, just under the newer component name and now with volume bars added as a third series on the same chart. **Explicitly not a new derived metric** — `paper_leverage` is TOTAL COMEX open interest across every listed contract month ÷ registered inventory, not scoped to the front-month contract specifically the way curve spread is; a true front-month-only OI/physical ratio would need CME's real per-contract-month OI breakdown, confirmed as a paid Market Data Platform product and permanently out of scope (same gap `delivery_behavior.py`'s `contract_month_oi` gap documents under Tab: Inventory). This chart pairs two real, independently-computed AV series for visual comparison, it does not manufacture a ratio AV has no free path to compute. Merges on curve spread's own dates via **nearest-date matching within 3 days** (not exact-date) — confirmed live that only ~70 of 290 real curve-spread dates had an exact same-calendar-day `paper_leverage` match (the two series' underlying update cadences don't land on the same days very often), so exact-date joining produced a badly gapped leverage line; nearest-date matching (same pattern `CombinedChart`'s `nearestGsr` already uses for pairing CoT against GSR) brought real coverage to 289/290 dates.
- **Combined CoT chart window selector**: `CombinedChart`'s originally-hardcoded 5-year view is now driven by the "Paper Games" panel-wide selector (see Panel contents above) — not its own window buttons, and not the intermediate `COMBINED_CHART_WINDOWS`/per-section-selector design that preceded the final panel-wide consolidation. Client-side filtering of the already-fetched series either way, never a per-window refetch. "All" reaches back to COT's real coverage start (2011-07-12), not an arbitrary large number. Added specifically so curve spread (which only accumulates forward from whenever this feature was deployed) and COT positioning (2011+) can be meaningfully viewed together across different depths of history.
- **Historical case log** (`squeeze_case_log` table, `GET /api/squeeze-cases/db`) — a small, hand-maintained SQLite table of known squeeze/dislocation cases for pattern reference, not a workflow (explicitly not using the Research tab's session/promote/dismiss lifecycle — "it's numbers fetched from an API that we do math on," too heavy for flat reference data). Seeded with 3 bootstrap cases: 2011 Silver Blow-off, 2020 COVID Gold Liquidity Crisis (a documented negative-ish control — liquidity panic, not a positioning squeeze, explicitly not conflated with the other two), and January 2026 Silver Squeeze (the feature's origin case). `curve_reading_snapshot` is nullable and left NULL for all three bootstrap rows — curve spread backfill for cases predating `futures_curve_spread`'s own ingestion start is best-effort, not guaranteed, per the spec. **DB + backend route only this pass — no frontend list/table yet** (see known gaps below).

### CoT: known gaps

- 📌 None currently open beyond the FND-approximation caveat above (inherited from Delivery Behavior's permanent contract-month-OI gap).
- 📌 LBMA fix (see Silver/Gold sections above): gold's PM fix (15:00 London) is not available from GoldAPI.io's free tier at all — only AM. `lbma_fix.price_usd` is not independently cross-checked against LBMA's own official print (paid IBA license required). GoldAPI.io's per-metal fix-moment timestamp is not trustworthy (confirmed live — both metals stamped identically regardless of real differing fix times). GoldAPI.io's free-tier monthly quota (500 req/month) was exhausted 2026-07 by manual investigation testing — `lbma_fix` won't receive new real data until the next billing cycle resets it or the plan is upgraded; a fix for a real weekend-mislabeling bug (see above) is confirmed correct offline but not yet re-verified against a live fetch. Yahoo Finance (curve spread's source) was investigated and confirmed unable to substitute — the LBMA fix is an auction-set benchmark, not a traded instrument Yahoo's chart API carries.
- 📌 Leverage history backfill (see Silver/Gold sections above): silver's real ceiling is 2020-01-02, gold's is 2026-02-17 — both set by when metalcharts.org's registered/eligible split itself starts being reported, not by CoT's 2011 coverage or `total`'s 1992 coverage. Gold's backfill is close to a no-op today as a result. No known path to extend either further back — this is an upstream data gap, not a fetch-shape problem.
- 📌 `db.get_volume_series`'s real history is thin (11 days as of the Silver rebuild) and has no backfill path — same underlying constraint as the old `volume_oi`/`gold_volume_oi` daily series always had (metalcharts.org's `volume-oi` endpoint has no historical range support, confirmed live). `SilverLeverageCurveVolumeChart`'s volume bars will only show real data on whatever recent dates happen to be in that window; this grows forward one row at a time, same as before.
- 📌 `price-spec.md` (the companion spec doc for this feature) still describes the pre-fix design (CFTC backfill + metalcharts.org daily stitching) — not updated as part of the 2026-07 CFTC-only fix. This section is now the canonical description of how leverage actually works; treat `price-spec.md`'s Section 1 as historical/superseded until it's revised.
- 📌 metalcharts.org's `volume-oi` endpoint's `openInterest` field is confirmed unreliable for this purpose (~15% below CFTC's real figure, consistently) but still fetched every slow-tier cycle purely to reach its `volume` field, which has no CFTC equivalent. Root cause of the OI discrepancy itself (which trader category metalcharts.org is excluding) was never confirmed — flagged as a real open question, not just a "known gap" to route around, in case it ever matters for a different feature that might otherwise be tempted to reach for metalcharts.org's OI number.
- 📌 `squeeze_case_log` has no frontend view yet — DB + `GET /api/squeeze-cases/db` only, per squeeze-context-spec.md Story #3's explicit scope for this pass. A read-only list/table is a documented follow-up, not built now.
- 📌 `futures_curve_spread`'s real backfill depth is bounded by how far back Yahoo serves daily history for a given contract symbol (~1 year observed) and by `CURVE_SPREAD_CANDIDATE_MONTHS_BEHIND`'s 14-month trailing sweep — real coverage today is 2025-05 through present. This is a real per-date-ranked backfill (see the Squeeze Context subsection above for the two bugs that were fixed to make it real), not a single mislabeled symbol pair, but it still can't extend further back than Yahoo's own retained history per symbol. This is why the historical case log's `curve_reading_snapshot` is NULL for the 2011 and 2020 bootstrap cases (predate any possible backfill) — the January 2026 case is within real coverage and could have a `curve_reading_snapshot` backfilled from `futures_curve_spread` directly, not yet done.
- 📌 Spot-vs-front-month spread (a more sensitive alternative to front/next paper-contract spread — see the Squeeze Context subsection above) is not yet built. AV's own real spot-price tick history (`spot_price_tick`) doesn't reach back far enough to backtest it against the January 2026 squeeze (only one real tick exists in that entire window, confirmed live) — it would only be meaningful accumulating forward from whenever it's added, not retroactively.
- 📌 The per-date contract-liquidity ranking (front = highest real volume that day; next = highest-volume candidate with a strictly later delivery month) is real and delivery-order-enforced as of the fixes above, but still a live-computed ranking, not a cached/validated assumption — if COMEX's real liquidity distribution ever produces an unusual 3-way-close volume tie right at a rollover boundary, the picked front/next could shift in a way not fully stress-tested beyond the 2026-07 investigation and its two bug fixes. Worth revisiting if `futures_curve_spread`'s `front_month_symbol`/`next_month_symbol` columns are ever observed flapping unexpectedly.
- 📌 `GET /api/cot/db`'s `signal_track_record` field (server-computed via `pipeline/compute.py`'s `compute_signal_track_record`) is still computed on every request but is **no longer consumed by the frontend** — `SignalTrackRecord` and its `ZoneTable`/`HitRateRow` helpers were removed from `silver_cot_tracker.jsx` entirely (see the Silver/Gold sections bullet above), not just hidden. The backend computation itself was left in place (cheap, and other consumers of `/api/cot/db` besides the frontend could still want it) — not a bug, just an intentionally asymmetric removal (frontend-only) worth knowing about if `signal_track_record`'s continued presence in the API response is ever questioned.

---

## Tab: Money Supply

**Frontend**: `frontend/src/money_supply.jsx` (imported directly into `App.jsx`, not nested under another panel). **Backend**: `main.py`'s FRED routes.

### Money Supply: what it does

Tracks the supply of the thing being debased, as monetary context for the metals panels.

**Panel structure — exactly two levels of nesting, not three**: the panel header reads **"Dollars and Sense"** (renamed from "Money Supply" at the user's request — the App.jsx nav tab itself is still labeled "Money Supply," this rename is scoped to the in-panel title only) — a plain, **non-collapsible** `<div className="comex-panel">` header, not its own `<details>`. A first version wrapped this header in a collapsible outer level too (3 levels of nesting total: Dollars and Sense → 4 sub-panels → each sub-panel's own content), which the user flagged as too much nesting — flattened to 2 levels by removing the outer `<details>`/`<summary>`/`collapsible-pane-body` entirely, leaving `<div className="comex-panel">` as a plain wrapper. Inside it, four independently-collapsible sub-panels are the *only* collapsible level (each its own `<details className="collapsible-pane" open>`, same pattern as Inventory's nested Delivery Behavior panel): **M2 Money Stock / Fed Balance Sheet**, **Fed Balance Sheet Composition**, **QE / QT**, **Dollars vs Silver vs Gold as Purchasing Power** — all default open. The window/Custom-range/Refresh controls live in the (non-collapsible) "Dollars and Sense" header, not inside any one sub-panel, since they drive all four sections' shared `window_` state — nesting them inside a collapsible sub-panel would hide them for the other three sections whenever that one sub-panel was collapsed, a real usability bug caught before shipping.

Each collapsed sub-panel's `<summary>` surfaces a compact supplementary-data line so collapsing doesn't hide the headline numbers: **"M2 Money Stock / Fed Balance Sheet"**'s own summary (not the outer panel header — moved there per the two-levels-only restructure) shows M2 (trillions) and Fed Balance Sheet as % of M2; **"Fed Balance Sheet Composition"**'s summary shows the current Assets:Liabilities ratio; **"QE / QT"**'s summary shows the Assets total and its most recent weekly change (green if positive, red if negative); **"Dollars vs Silver vs Gold as Purchasing Power"**'s summary shows the current baseline plus gold/silver's return relative to it. The stale-data warning (inside the M2 sub-panel's body) was extended at the user's request to include the actual last-known values, not just the dates — "M2 last reported 2026-05-01 ($23.1T) ..." / "Fed Balance Sheet last reported 2026-07-15 (29.3% of M2) ...", so a stale reading still tells you what the last real number was, not just how old it is.

**Standing rule, applied to all four summaries**: when a date is pinned (see Cross-chart pin below), every section's summary reflects that pinned date's values, not "latest" — each summary computes its own row via that chart's already-existing pin-snap variable (`pinnedDateMerged`/`pinnedDateComposition`/`pinnedDateQeQt`/`pinnedDateMetals`) when `pinnedDate` is set, and only falls back to the true latest-real-reading logic (`m2LatestValue`/`lastPctRow`, or simply the last row of that chart's own merged array) when nothing's pinned. Composition's own pie/tooltip lookup (`compositionPieRow`) was extended to prioritize an explicit pin over a live hover over "latest" (pin > hover > latest) so clicking elsewhere to set a pin isn't silently overridden by whatever the mouse happens to be sitting on. The M2 sub-panel's stale-data ⚠ warning is suppressed while a date is pinned, since "stale" describes the true latest reading, not whatever historical date is currently pinned.

**Time window**: 2Y/5Y/10Y/20Y preset buttons (2Y is the page-load default) plus a **Custom** button that reveals From/To date inputs — `window_="custom"` doesn't fetch until both bounds are set and `start <= end` (picking Custom alone, or only one date, doesn't fire a request with no real range). `GET /api/fred/money-supply/db` and `GET /api/metals/prices/db` both accept `window`, and — only when `window=custom` — `start`/`end` (`YYYY-MM-DD`); `start` becomes the SQL `since` bound directly, `end` trims the already-fetched rows in Python (`db.get_fred_observations` itself has no upper-bound param, so each route does its own trimming via a local `_trim`/inline filter rather than adding an `until` param to that shared helper). Same window (including custom start/end) drives every chart in this panel — M2/WALCL, Fed Balance Sheet Composition, and the Dollars-vs-Metals purchasing-power chart all share one `window_` state and refetch together.

**A real bug in the custom range, caught by the user right after it shipped** ("no M2 YoY stats earlier than 2021?"): the custom branch originally set `fetch_since = start` with zero extra lookback, so `_compute_yoy` never had the 12 prior months it needs — every M2/WALCL `yoy` value inside a custom range came back `null` regardless of how far back `start` was. Fixed by fetching 2 extra years before `start` in the custom branch too, same margin the preset branches already use — verified against a 2007–2010 range, YoY now correctly starts computing at 2007-07-01, exactly the first date with a full trailing 12 months of real M2 data (M2SL's own real FRED coverage starts 2006-07-01).

**Cross-chart pin (click-to-highlight)**: clicking a point on the M2/WALCL, Fed Balance Sheet Composition, or QE/QT chart sets a shared `pinnedDate`. Clearing is a **click on the 📌 pinned-date button itself** — a button styled like the 2Y/5Y/etc. window presets, sitting to the left of them in the panel header, showing `📌 {pinnedDate}` and clearing on click. **Right-click-to-clear was tried first and dropped** — Safari (and browsers generally) intercepts `contextmenu` for its own right-click menu in ways that made the in-app clear handler unreliable for the user in practice, so a real click target replaced it rather than fighting the browser's native gesture. Every chart in the panel — including the Dollars-vs-Metals purchasing-power chart, which doesn't originate a pin via click since its mousedown/mouseup are already claimed by its own press-and-hold "since that date" comparison gesture, but still displays a pin set elsewhere — draws a `ReferenceLine` at that date. Because M2/WALCL, Composition, and the metals chart each have a different date grid (monthly-anchored, weekly-anchored, monthly-resampled) that rarely share exact date strings, and Recharts' category-axis `ReferenceLine` only renders when its `x=` value exists verbatim in that chart's own dataset, each chart snaps the shared `pinnedDate` to its own nearest real row date on/before it (`nearestRowDate` — falls back to the nearest row after it if `pinnedDate` predates that chart's own data) before drawing the line, rather than passing the raw pinned string to every chart and having it silently fail to appear on 1-2 of them. The QE/QT chart snaps against its own filtered dataset (`qeQtRows`, only rows with a real `assetsChangeBillions`), not the full `composition` array the chart above it uses, since those are two different date grids too.

**Pinned tooltips**: when a date is pinned, each chart also renders a fixed tooltip box directly below it (same "below the chart" placement the Composition pie's hover-ELI5 box already used) showing that chart's real values at the snapped date — not floating exactly over the `ReferenceLine` itself, which would require reaching into Recharts' internal per-chart pixel-scale math (fragile across the panel's mix of dual-axis/stacked/category charts) for a purely cosmetic gain. Recharts 3.x's `<Tooltip>` has no clean "force open at date X" API without synthesizing mouse events, so each chart's tooltip content was pulled out into its own named component (`MoneySupplyTooltip`, `CompositionTooltipContent`, `QeQtTooltipContent`, `MetalsTooltip`) that both the real hover-driven `<Tooltip content={...}>` and the pinned box call directly with `active` forced `true` and `label` set to the snapped pinned date — one source of truth for each chart's tooltip content, two triggers (hover, pin) for when it's shown.

**A real bug caught by the user right after this shipped** ("no M2 YoY stats earlier than 2021?"): the custom-range branch originally set `fetch_since = start` with zero extra lookback, so `_compute_yoy` never had the 12 prior months it needs to diff against — every M2/WALCL `yoy` value inside a custom range came back `null` regardless of how far back `start` was, which read as "no YoY data before [whenever the 2-year preset's own lookback happened to reach]." Fixed by fetching 2 extra years before `start` in the custom branch too, same margin the preset branches already use. Verified against a 2007–2010 custom range: YoY now correctly starts computing at 2007-07-01 — exactly the first date with a full trailing 12 months of real M2 data (M2SL's own real FRED coverage starts 2006-07-01), not a moment later than the underlying data actually allows.

- **M2 Money Stock / Fed Balance Sheet** — trillions USD, stacked area chart (left axis) + M2 YoY % (right axis). M2's full area (green) is the total money stock; WALCL is shaded in **red as the share of that same area it takes up**, not a separate line plotted against its own axis — "roughly a third of the green M2 area is red" is meant to read directly off the chart, not require mentally comparing two lines' heights. Two dollar-scale fields stack to exactly M2's total: `walcl_share` (WALCL's own value, red, drawn first/bottom) and `m2_remainder` (M2 minus that, green, stacked on top) — both computed in `mergeSeries`, replacing an earlier version that plotted WALCL as a right-axis % line instead (changed after the user asked for a shaded-portion-of-the-area visual specifically, matching a "roughly 1/3 red" mental model they described directly). `walcl_pct_m2` is still computed and still shown in the tooltip alongside the raw dollar figures, just no longer has its own chart line.
  - **Legend matches the Composition pie's legend look/behavior** — `M2_LEGEND_SERIES` (a `COMPOSITION_SERIES`-shaped array: `legendLabel` for the compact row, `eli5` for the full explanation, `dashed: true` for the M2 YoY % entry's dashed-outline swatch) replaces what used to be an always-expanded vertical `comex-legend-list` with full-sentence prose inline. Now horizontal (`comex-legend-list--horizontal`) with click-to-toggle rows (`clickedM2Key` state, same pattern as Composition's `clickedPieKey`) — clicking a row shows its full explanation in a note below the legend, clicking the same row again hides it.
  - **Three real bugs caught by the user in this chart, all fixed**: (1) "WALCL plummets and M2 spikes on 7/1/2025" — M2 stamps on the 1st of the month, a date WALCL (weekly) essentially never has its own row for; the original `m2_remainder` fell back to the FULL raw M2 value whenever that exact row's `walcl` was null instead of M2 minus the most recent real WALCL reading, collapsing the red band to 0 and spiking the green band to M2's full total on every M2-only row even though neither series had actually moved. Fixed by forward-filling `lastWalcl` the same way `lastM2` was already forward-filled for `walcl_pct_m2`. (2) "M2 is spiky" — even after fix (1), `walcl_share`/`m2_remainder` were still only non-null on the ~1-in-20 rows where M2 itself reports; Recharts' `stackId` math doesn't interpolate across null rows the same smooth way a plain `connectNulls` line does, so a stack with real values on only the 1st of each month rendered visibly jagged even though the real M2 series climbs smoothly (confirmed against raw FRED data — no real spikiness exists). Fixed by forward-filling M2 too (`lastM2`, used only for the stacked-area geometry), so both fields have a real number at every x-position; the raw, non-forward-filled `m2` field is kept separate and still used for the M2 YoY line and tooltip, which correctly show "—"/bracket text on days M2 didn't actually report rather than a fabricated same-as-last-month value. (3) "we've manufactured data that hasn't been released yet" — fixes (1)/(2)'s forward-fill had no upper bound: M2's real last print (confirmed live, ~2026-05-01) runs roughly 2.5 months behind WALCL's (~2026-07-15), so the forward-filled stack kept repeating May's stale M2 figure all the way through July, which really did look like fresh data that didn't exist. Fixed by capping the forward-fill at each series' own true last real date (`lastRealM2Date`/`lastRealWalclDate`, taken from the actual last row of the raw `m2`/`walcl` arrays) — once a row's date passes that cutoff, the filled value goes back to `null` and the chart correctly shows a gap rather than a repeated stale number, matching AV's standing nulls-over-zeros convention (forward-fill bridges interior gaps between two real readings; it does not extend a series past its own real coverage).
- **Fed Balance Sheet Composition** (per `specs/fed-balance-spec.md`, fully implemented) — a look inside WALCL's top-line number: all 5 of FRED's H.4.1 candidate series — Bank Reserves (WRESBAL), Overnight Reverse Repo Facility (RRPONTSYD), Treasuries Held (WSHOTSL), MBS Held (WSHOMCB), Discount Window/Primary Credit Lending (WLCFLPCL) — raw trillions USD, one shared chart section below the M2/WALCL chart (kept separate rather than added to that chart's dual axis, so its scale isn't disturbed by five much-smaller-magnitude lines). Shares the panel-level 2Y/5Y/10Y/20Y window selector with the M2/WALCL chart above it (one fetch, one `window` param covers both — not an independent control). Raw value only, no YoY.
  - **Split into Assets vs. Liabilities, not one flat total** — a real correctness bug caught by the user, not a design choice from the start. FRED's own `/fred/series` titles confirm WSHOTSL/WSHOMCB/WLCFLPCL are `"Assets:"` line items (these three roughly track WALCL, though not exactly — AV doesn't track every WALCL line item, e.g. repos/other credit facilities/gold certificate account, so the Assets total runs a bit below WALCL by design) while WRESBAL is explicitly `"Liabilities:"` — the opposite side of the same balance sheet, not a slice of the asset total. RRPONTSYD is a separate temporary-open-market-operations mechanism, grouped with Liabilities as balance-sheet-adjacent but not itself an `"Assets:"` series either. An earlier version summed all 5 into one "Total Balance" stacked chart and one pie, which silently double-counted assets against liabilities — confirmed live on 2023-03-22, summing all 5 gave ~$13.75T against WALCL's real $8.73T that same week.
  - Three view modes via a "Assets + Liabilities"/"Assets Total"/"Liabilities Total" toggle (**Assets + Liabilities is first and the page-load default** — the most complete single view): **Assets + Liabilities** (both stacks rendered together — two independent `stackId`s, `composition-assets`/`composition-liabilities`, never merged into one combined stack, since summing them was the exact bug the group split fixed in the first place), **Assets Total** (stacked area, WSHOTSL+WSHOMCB+WLCFLPCL only), **Liabilities Total** (stacked area, WRESBAL+RRPONTSYD only). **A fourth mode, "By Series" (checkbox-per-line, unstacked), was removed entirely at the user's request** ("I don't see using that") — not hidden, actually ripped out: the toggle button, the per-series `compositionVisible` state/checkboxes, the unstacked `<Line>`-per-series chart-rendering branch, and the corresponding tooltip/pie-series branches that only existed to serve that mode are all gone, not just unreachable. A fixed pie-chart panel below the line/area chart shows the composition breakdown for whatever date is hovered, scoped to whichever group is active (all 5, un-grouped, in "Assets + Liabilities" view) — since RRPONTSYD is genuinely daily but the other four are weekly-as-of-Wednesday, hovering any day snaps the pie (and the line-chart tooltip) to the nearest Wednesday reading on/before the hovered date, same "nearest date on/before" convention `VaultSnapshotPanel` uses for its own pinned-date lookups, so the pie never renders with missing slices and the tooltip never shows blank values on a non-Wednesday hover.
  - **"Total Assets" line was spiky — the same root cause and fix as the M2/WALCL chart's spikiness bug above**, just in `mergeComposition` instead of `mergeSeries`: `totalAssets`/`totalLiabilities` were computed from each row's own raw per-series values, which are null on ~4 out of every 5 rows (RRPONTSYD is daily, the other four are weekly-as-of-Wednesday) — Recharts' `stackId` math doesn't interpolate across those nulls smoothly, so the stack rendered jagged even though the real FRED series climb/decline smoothly. Fixed with the same forward-fill approach: a `${key}_filled` field per series (e.g. `wshotsl_filled`), forward-filled and capped at that series' own true last real date (same cap as the M2/WALCL fix, so this doesn't reintroduce the "manufactured data" problem either) — the Assets/Liabilities/Both stacked-area `<Area>` components render from `${key}_filled`.
  - **Legend is a fixed list below the pie, not on-slice labels** — went through two iterations. First version put each series' `legendLabel` directly on/around its slice via a custom Recharts `label` function (with a leader line via `labelLine`), replacing an even earlier separate `comex-legend-list` below the chart. **That on-slice version had a real bug the user caught**: labels would disappear whenever the pie's data moved (a new hovered/pinned date, a slice crossing in/out of the `>0` filter, etc.), since label position is computed from live slice geometry that was itself changing mid-render. Reverted to a fixed legend below the chart — position independent of slice geometry, so it can't disappear that way — using this codebase's established "custom legends" convention (`comex-legend-list`/`comex-legend-item`, same classes used by the metals-comparison chart's own clickable legend) rather than Recharts' own label/Legend components.
  - **Legend rows are click-to-toggle for the ELI5 tooltip, not hover** — a second real bug caught along the way: an earlier version used a native HTML `title=` attribute for the ELI5 text, which is unreliable (long hover delay, easy to miss the trigger area). Fixed with `clickedPieKey` (real React state, renamed from an earlier `hoveredPieKey` once the trigger moved from hover to click) — clicking a legend row shows its ELI5 text in a note below the legend; clicking the same row again hides it. The pie's `<Cell>`s themselves have no click handler of their own (every interaction still originates from the legend rows) — but clicking a legend row now also visually highlights that series **on the chart**, at the user's request: the matching `<Cell>` gets a light stroke outline + full opacity while the other slices dim, and — same `clickedPieKey` driving the Assets/Liabilities/Both stacked `<Area>`s too — that series' area gets a thicker stroke and fuller fill opacity while the rest dim. The M2/WALCL chart's own legend (`clickedM2Key`) got the identical treatment on its two `<Area>`s and the M2 YoY `<Line>` (stroke width + opacity, using `strokeOpacity` for the line specifically since it has no fill to dim).
  - **Legend always lists all 5 series, not just whichever the pie is currently showing** — a real gap the user caught: the legend was iterating `compositionPieData`, which is filtered to `value > 0`, so a near-zero series (RRPONTSYD is currently `$0.0T` — confirmed live) dropped out of the legend right along with its slice. Fixed by sourcing the legend from `COMPOSITION_SERIES` directly instead (it already carries `color`/`legendLabel`/`eli5` unconditionally for every series, pie-visibility-independent), so all 5 series always have a legend row and a clickable ELI5 tooltip regardless of the current view mode or whether that series' current value is large enough to render as its own slice.
  - **Legend spans horizontally, not vertically, and is centered under the pie** — `.comex-legend-list--horizontal` (index.css) overrides the base `.comex-legend-list`'s `flex-direction: column` stack to `row` + `flex-wrap: wrap` + `justify-content: center`, so all 5 series' legend rows sit in one centered horizontal band (wrapping to a second row on narrow viewports) instead of a tall vertical list.
  - **Pie chart sits beside the line chart, not below it, in its own full-width row** — went through three rounds of dead-space fixes, each one caught by the user as still not tight enough. (1) Capped the pie's own `ResponsiveContainer` from `width="100%"` (a fixed-size circle stretching a full-width container just leaves empty space either side) to a fixed `width={320}`, centered — still left a mostly-empty full-width row below the line chart. (2) Restructured to a side-by-side flex layout: line chart and pie share one `display: flex` row (`flexWrap: "wrap"` so they still stack on narrow viewports), pie's own circle shrunk to fit its narrower column (`outerRadius={80}`, container `240×240`, column flex-basis `260px`) — still too much buffer around the circle itself per the user. (3) Shrunk everything again in the same direction (column flex-basis `180px`, container `180×180`, `outerRadius={70}`/`innerRadius={36}`) and zeroed the `<PieChart>`'s own `margin` prop, since Recharts reserves default chart margins on top of the container size that were adding extra buffer beyond what the flex column itself accounted for. The horizontal legend (see above) stays full-width below both columns, since it needs the room to lay out all 5 series in one row.
  - **"As of {date}" caption beneath the pie** — the pie is a single-date snapshot (`compositionPieRow`), unlike the line chart beside it which shows a range, so without this there was no way to tell which date the slice sizes actually reflect (the pinned date, the hovered date, or the latest reading if neither is set). Sits inside the pie's own flex column, below the `ResponsiveContainer`.
  - **The "as of [date]. Hover the chart above..." panel-note was removed** — redundant once every collapsible sub-panel's own `<summary>` started surfacing its as-of date/values directly (see below), per the user's request to drop it.
  - **Panel copy shortened** — the Composition intro note and the QE/QT note were both cut down substantially (from several sentences of ELI5-level explanation to 1-2 short sentences) after the user flagged them as "too wordy." The per-series ELI5 explanations themselves weren't cut — they just moved into the pie's hover tooltip (see above) rather than staying inline as always-visible legend prose.
  - **Colors are group-coded**: Liabilities series (WRESBAL/RRPONTSYD) get a reddish palette, Assets series (WSHOTSL/WSHOMCB/WLCFLPCL) get a greenish one — each series keeps its own distinct shade within that family (still individually distinguishable in the pie's per-slice tooltip and the always-all-5 legend below it), but the group reads at a glance, matching the M2/WALCL chart's own red-share-of-green-area convention above.
  - **Assets:Liabilities ratio** — a dashed right-axis line (`assetsLiabilitiesRatio = totalAssets / totalLiabilities`) plotted alongside the Assets Total/Liabilities Total/Both stacked areas. Deliberately not clamped — when Liabilities is near zero (RRPONTSYD usage has largely dried up), the ratio can swing to a large multiple; that's a real reading of the data, not a display bug, so it's left unclamped rather than capped/smoothed. Displayed in the Composition sub-panel's `<summary>` as `Assets / Liabilities: {ratio}:1` (changed from an earlier `Assets:Liabilities {ratio}x` format at the user's request) — colored green (`WIN_COLOR`) above 1.25, red (`LOSS_COLOR`) at or below it. Real current value (confirmed live): assets $6.46T ÷ liabilities $3.14T ≈ 2.1:1, comfortably in the green range.
  - **`CompositionTooltipContent` shows detailed per-series totals, not just the two group sums** — at the user's request, each hover/pin tooltip now lists every individual series' own value (`row[`${key}_filled`]`, the same forward-filled/capped field the charts themselves render, not the raw possibly-null `row[key]`) indented under its group header, not just "Total Assets: $X.XT" / "Total Liabilities: $X.XT" with no breakdown. **Assets group and its series are colored green (`WIN_COLOR`), Liabilities group and its series are colored red (`LOSS_COLOR`)** — a real color-consistency fix along the way, since the previous version colored the single-group ("assets"/"liabilities" view) total with `WALCL_COLOR` (a red) regardless of which group was actually shown, and the "both" view's two totals with individual-series colors (`WSHOTSL_COLOR`/`WRESBAL_COLOR`) rather than a clean group color. Which group(s) render is still scoped to the current `compositionView` — "assets"/"liabilities" views only show their own group's breakdown, "both" shows both.
  - **"QE / QT" chart** — a bar chart of week-over-week change in the Assets total (left axis, billions USD, not trillions), below the pie. Each week's bar is colored **green if assets grew that week ("QE"), red if they shrank ("QT")** — per-`Cell` coloring (`WIN_COLOR`/`LOSS_COLOR` by sign), not a single fixed series color; originally a step-`Area` in one flat color, changed to a `Bar` specifically because Recharts only supports per-point fill coloring on `Bar`/`Pie`, not `Area` — applying `UI_STANDARDS.md`'s green/red convention properly required the switch. Diffed against the previous row that actually has a `totalAssets` reading (the prior Wednesday), not the literal previous array row, since most rows are RRPONTSYD-only daily rows with no assets reading to diff against. **A real precision bug caught by the user** ("hasn't the balance sheet been flat since 5/3/2023?" — it hadn't; FRED's own data shows continuous decline): real weekly changes are usually in the $1B–$150B range, well below `round1`'s 1-decimal-in-trillions precision (i.e. $100B buckets), which silently rounded nearly every real week's change down to 0.0, making a genuinely-moving chart look flat. Fixed by computing `assetsChangeBillions` directly in billions (`Math.round(diff * 1000)`, no `round1`) instead of reusing the trillions-scale field. **Also carries a `totalAssets` line on a second right axis (trillions USD, dashed)** — the balance sheet total itself, plotted alongside its own week-over-week change, added at the user's explicit request so the level and the momentum are visible together on one chart rather than needing to cross-reference the Assets Total chart above it.
  - **QE/QT legend brought up to `UI_STANDARDS.md`** — previously had no legend at all despite having two distinct series (the weekly-change bars and the `totalAssets` line). Added `QE_QT_LEGEND_SERIES` (same `legendLabel`/`eli5`/`dashed` shape as `M2_LEGEND_SERIES`/`COMPOSITION_SERIES`) and `clickedQeQtKey` state — horizontal legend, click a row to toggle its full explanation below and highlight that series on the chart (clicking "Weekly Change" dims the `totalAssets` line; clicking "Balance Sheet Total" dims the bars' opacity, since the bars are already individually green/red and can't be "recolored" to indicate selection the way a single-color line can). The "Weekly Change" legend swatch uses a two-tone CSS `linear-gradient` (half `WIN_COLOR`, half `LOSS_COLOR`) rather than one flat color, since the real series has no single color — it's green or red per week. Tooltip colors (`QeQtTooltipContent`) also fixed to match: the balance-sheet-total line changed from `WSHOTSL_COLOR` (an unrelated series-specific hue) to the neutral `RATIO_COLOR`, and the weekly-change figure now colors green/red by its own sign instead of a flat neutral color.
  - Legend rows use a plain-English `legendLabel` (no FRED acronym); the pie chart and line-chart tooltip still use the acronym-bearing `label` so the series can be tied back to its real FRED series_id.
  - **Each series' `eli5` explains how it connects to the others and to the overall money stock**, not just what the series is in isolation — expanded at the user's request. Real relationships documented: WRESBAL (Bank Reserves) rises/falls with WSHOTSL+WSHOMCB purchases (the Fed pays for bond buys by crediting reserve accounts) but doesn't automatically become M2 growth unless banks actually lend it out; RRPONTSYD (Reverse Repo) and WRESBAL act as two connected liability-side buckets cash shifts between, with no direct effect on the Assets series; WSHOTSL/WSHOMCB (Treasuries/MBS Held) together make up nearly all of Assets and by extension most of WALCL itself, and MBS lags Treasuries' QT decline because mortgage prepayment (not a fixed maturity) is what retires those holdings; WLCFLPCL (Discount Window) is explicitly temporary — it credits reserves the same way any Fed asset purchase does, but is expected to unwind quickly once bank stress passes, unlike a durable QE-driven reserve increase.
  - **RRPONTSYD has a genuine ~9-month gap of NO real FRED data at all** (2006-07-19 through 2007-04-26 — confirmed live, its earliest real non-null reading is 2007-04-26), not just an off-cadence-day gap like the other four series' Wednesday-only cadence. `findNearestCompositionRow`'s "every series in the group must be present" check originally included RRPONTSYD for the liabilities group, which meant hovering anywhere in that ~9-month window returned nothing at all, even though WRESBAL (the other liability series) has perfectly good real data there — a real bug the user caught by hovering 1/29/2007 specifically. Fixed by excluding RRPONTSYD from `requiredKeys` entirely (both the liabilities-group case and the ungrouped/"Assets + Liabilities" case) — RRPONTSYD still renders in the pie/tooltip whenever it has a real value, it's just no longer a blocking requirement for the lookup to succeed.
  - **Confirmed real, not a bug**: the week of 2023-03-15 shows a real, sharp WLCFLPCL spike (Discount Window Lending, $4.6M → $152.9M) plus a real WRESBAL jump ($3.0B → $3.25B) — this is the Silicon Valley Bank / Signature Bank collapse week (SVB failed 2023-03-10), banks borrowing emergency short-term cash directly from the Fed plus the Fed's Bank Term Funding Program liquidity response launching the same week. Exactly the "spike here usually means a bank is under stress" signal WLCFLPCL's own ELI5 text describes — a real historical event visible in the data, not a data-quality issue.
- **Dollars vs Silver vs Gold as Purchasing Power** — four series (Fiat $100 nominal / Gold XAU / Silver XAG month-end closes since 2006 / CPI-derived Purchasing Power) as relative return % against a user-selected baseline. Click a legend label to set baseline (Purchasing Power can't be baseline — "not a holdable asset"); checkbox toggles visibility. Click-and-hold a chart point to see "since that date to latest" return in the tooltip (green/red vs. baseline), without redrawing the lines.
  - **Legend spans horizontally**, per `UI_STANDARDS.md` — `comex-legend-list--horizontal` applied to the wrapping list, same as the Composition/M2/QE-QT legends. This legend keeps its own distinct interaction model rather than converting to the click-for-ELI5-popup pattern those legends use: clicking a row here already does something load-bearing (sets the baseline, re-running the real rebase math), and the checkbox already controls chart visibility directly — both are more functional than a dim/highlight pair, so they were left as-is at the user's explicit request ("keep the comparison math"). `.metals-legend-row`'s rows carry more per-row content (a checkbox, a baseline-select button, a conditional "— baseline (X at 0%)" note) than the simpler swatch+label legends elsewhere, so `.comex-legend-list--horizontal .metals-legend-row` overrides the base `.legend-btn-row { flex: 1 }` full-row-stretch rule to `flex: 0 0 auto` — otherwise each row would fight to fill the remaining horizontal space in the flex row instead of sizing to its own content and wrapping naturally.

### Money Supply: data flow

`GET /api/fred/money-supply/refresh` fetches M2SL/WALCL/CPIAUCSL/WRESBAL/RRPONTSYD/WSHOTSL/WSHOMCB/WLCFLPCL from FRED, upserts `fred_observations` (generic `(series_id, date, value)` store, not FRED-exclusive — also holds Yahoo-sourced metal closes, see below). `GET /api/fred/money-supply/db?window=2y|5y|10y|20y` reads it back, computes YoY + CPI-derived purchasing-power index server-side, returns trillions/percent/index (never raw FRED units). Requires `FRED_API_KEY` in the environment (`.env` via `python-dotenv`, or shell-exported); `refresh` raises a clean `500` if unset.

**Unit note, confirmed live against FRED's `/fred/series` metadata**: the 5 Fed Balance Sheet Composition series do **not** all share a native unit despite all being simple weekly series — WRESBAL/WSHOTSL/WSHOMCB/WLCFLPCL report in millions of USD (same convention as WALCL), RRPONTSYD reports in billions (same convention as M2SL). `/api/fred/money-supply/db` converts each with its own correct divisor (`_millions_to_trillions` helper for the four millions-denominated series); assuming a shared divisor was a real bug caught during build (an early version divided WRESBAL by 1,000 same as RRPONTSYD, producing a nonsensical ~$2,951T bank-reserves figure).

**Metal price history** (for the purchasing-power chart): `/api/metals/prices/refresh` fetches DAILY closes from Yahoo Finance (`SI=F`/`GC=F`, httpx directly in `main.py` — a separate code path from `pipeline/price_fetch.py`, which is a stdlib-only weekly-interval helper for the CoT pipeline specifically), resamples to one row per calendar month (last trading day on/before month-end, avoiding Yahoo's `1mo` bucket which includes the current in-progress month), upserts into `fred_observations` under `XAG_CLOSE`/`XAU_CLOSE` keys. `/api/metals/prices/db?window=...` returns raw price + index-to-100-at-window-start; the frontend does its own further client-side rebasing against the user-selected baseline.

### Money Supply: known gaps

- 📌 Fed Balance Sheet Composition ships raw value only — no YoY computed yet (unlike M2/WALCL). All 5 candidate H.4.1 series from `fed-balance-spec.md` are wired up; a YoY treatment remains the one open item, not committed.

---

## Tab: Inventory ("Stock & Flow" + Delivery Behavior)

**Frontend**: `frontend/src/comex_inventory.jsx` (top-level `ComexInventoryDashboard`) + `frontend/src/delivery_behavior_panel.jsx` (nested inside it) + `frontend/src/market_balance.jsx`. **Backend**: `backend/main.py`'s exchange-inventory routes + `backend/delivery_behavior.py`.

Structured as nested collapsible `<details>` panes: top-level "Stock & Flow" contains `CrossExchangePanel`, then separate collapsible sections for "COMEX — New York", "SHFE — Shanghai", "Market Demand", "Demand Composition Over Time", and "Global Context" (the latter three are siblings directly under Stock & Flow, not nested inside either exchange). `fetchAll()` calls only `/db`-suffixed routes — upstream refresh happens server-side via the tiered background loops (see Standing rules above), not on panel mount.

### COMEX — New York

- **Per-Vault Snapshot** (`VaultSnapshotPanel`) — today's registered/eligible/total by depository with day-over-day deltas + interactive pie chart. Supports being "pinned" to a past date (via hovering a row in Delivery Behavior's reclassification table below, wired through `handleHoverDate`) — reads `/api/silver/db/depositories?date=...` instead of latest-only, showing that date's real per-vault breakdown where history exists (accumulates only going forward from whenever the depositories fetch first ran — no upstream backfill exists for this specific route, unlike delivery notices' `ytd` trick below). The panel keeps its previous content mounted during a pinned-date fetch (never swaps to a shorter loading/empty tree, with a `min-height` lock + absolutely-positioned "no snapshot" overlay) — swapping the whole subtree caused Recharts' `ResponsiveContainer` to remount and briefly measure 0×0, collapsing the pie chart and knocking the mouse off the very row being hovered, canceling the pin.
- **Delivery Behavior** (nested collapsible, sibling of the snapshot — see full subsection below)
- **Registered vs Eligible** (nested, collapsible) — distinguishes warranted (deliverable) metal from eligible (stored, not warranted); sharp registered drops are a delivery-pressure signal. Includes a registered/eligible ratio series; spikes indicate metal being warranted for delivery.
- **Delivery Notices** (nested, collapsible) — daily issued/stopped, tabular. `mtdCumulative`/`ytdCumulative` omitted — always `0` in metalcharts.org's response (confirmed Feb–Jul, never populated); `dailyIssued`/`dailyStopped` are the real, working fields.

### SHFE — Shanghai

- **SHFE Warehouse Snapshot** — current warranted silver by approved warehouse, delta column, pie chart.
- **SHFE Silver Inventory** (nested, collapsible) — time-series, kg → troy oz (1 kg = 32.1507 oz).

### Cross-exchange overlay

- **COMEX vs SHFE Physical Exchange Inventories** — dual-axis (COMEX left, SHFE right, independent scale), PSLV/Sprott as reference line; proportional bar shows COMEX ~12× SHFE in absolute terms.
- **PSLV (Sprott Physical Silver Trust)** — live custodial oz from the Royal Canadian Mint, Ottawa, fetched directly from Sprott's API.

### Market Demand / Demand Composition (`market_balance.jsx`)

- **Annual Market Balance** (`MarketBalancePanel`) — Silver Institute annual supply/demand balance, deficit/surplus by year, 5yr cumulative, recoverable-stock runway estimates under stated assumptions (not a prediction). Reads `seed_data/silver_market_balance.json` directly (no DB, no external fetch) via a `__file__`-relative path; derived metrics computed on each request.
- **Demand Composition Over Time** (`DemandCompositionPanel`, separate export from the same file so it's independently collapsible) — stacked share of Industrial, Jewelry & Silverware, Physical Investment, ETF Net Flow demand by year.

### Global Context

- **Estimated Above-Ground Silver Stock** — proportional bar by category (Jewelry & Silverware, Investment/coins & bars, Industrial/unrecoverable, ETF & Exchange Vaults, Central Bank/Govt Reserves) from Silver Institute WSS 2024 / USGS / CPM Group, plus a live-computed bar for the AV-tracked exchange subset. All figures carry an explicit ±20% uncertainty qualifier. Staleness flag if survey >18mo old.
- **Stack Calculator** — personal troy oz holdings expressed as % of estimated above-ground stock, COMEX registered, and total tracked exchange inventory (COMEX + SHFE). Lives inside `GlobalSilverPanel`, which computes the live tracked total against the static above-ground total on every render (no hardcoded ratio).

### Delivery Behavior (`backend/delivery_behavior.py`, `frontend/src/delivery_behavior_panel.jsx`)

A cross-check layer on top of data the other tabs already persist (registered/eligible inventory, delivery notices, disaggregated CoT) — computes derived anomaly signals rather than owning new upstream fetches, except one (disaggregated CoT, described under the CoT tab above). Route: `GET /api/delivery-behavior/db?metal=XAG|XAU` (no `/refresh` — everything it reads comes from another tab's tiered refresh or from `pipeline/run.py`). Returns `{"reclassification": ..., "category_composition": ..., "deficit_context": ...}`.

- **Reclassification vs. Real Inflow** (`compute_reclassification_signal`, silver-only — `delivery_notices`/`inventory_aggregate` have no gold equivalent): walks `inventory_aggregate`'s day-over-day `registered` deltas, skips any day whose delta spans a `None`-registered gap or has no matching `delivery_notices` row for that exact date, flags a day if same-day delivery volume (`daily_issued + daily_stopped`, converted from COMEX contracts to troy oz — 5,000 oz/contract, a real unit-mismatch bug caught during build) covers less than 10% of the registered increase. Reads `delivery_notices` with `type="ytd"` (~85 days of history) not the `"mtd"` default (a handful of days-in-month) — confirmed live against metalcharts.org, so `main.py`'s slow tier specifically calls `_fetch_and_persist_delivery(type="ytd")` for this signal's coverage. Each flagged day also carries `total_oz`/`prev_total_oz`/`total_delta` (total vault holdings, not just registered — the two can move in opposite directions on the same day) so the UI table doesn't read as inconsistent against the aggregate chart above it.
- **Short-Term Anomaly vs. Structural Deficit** (`compute_deficit_context`) — repackages the annual Silver Institute balance series alongside the short-term reclassification window, so a short-term blip isn't mistaken for evidence about the multi-year structural deficit or vice versa.
- **Category Composition** — computed here (`compute_category_composition`) but rendered in the CoT tab, not this one, since it's CoT-derived data (see CoT tab above for full detail).
- **First Notice Day / Last Trade Day** (`first_notice_day`, `last_trade_day`, `last_business_day`, `days_to_fnd`) — computed on the fly, no seed table. Confirmed live that both silver and gold list monthly contracts on a rolling basis (silver: 26 consecutive months + Jul/Dec out to 60 months; gold: 26 consecutive months + Jun/Dec out to 72 months) — effectively every calendar month trades, not the small fixed cycle `deliveryBehavior-spec.md` originally assumed, so there's no small static list worth seeding, just a pure date rule. Last Trade Day (third-last business day of the delivery month) confirmed directly against COMEX rulebook Chapters 112 (silver)/113 (gold), saved to `seed_data/cme/112.pdf`/`113.pdf` — identical rule both metals. **First Notice Day required a real correction mid-build**: those rulebook chapters never use the term "First Notice Day" at all, and an early version incorrectly inferred FND = first business day of the delivery month. A verified real example (COMEX May 2026 silver, FND reported April 30, 2026 — the *last* business day of the *prior* month) proved that wrong; the corrected rule (FND = last business day of the month preceding the delivery month) is what's implemented. "Business day" is weekday-only (Mon–Fri), deliberately not holiday-aware — fine for an approximate seasonal marker, not an exact trading-calendar reference.

**Permanently out of scope, not deferred**: `contract_month_oi` (per-contract-month open interest/settlement) and the seasonal-baseline/OI-decay + price-lead-lag signals that depend on it (`deliveryBehavior-spec.md` stories #1/#5). CME's real per-contract-month OI/settlement data is a paid Market Data Platform product (`cmegroup.com/market-data/market-data-api.html`, tiered from $0.50/GB) — confirmed live, not a reverse-engineering gap. CME's site also runs aggressive, inconsistent Akamai bot-blocking independent of the paid-API question — even the free daily-bulletin PDFs (no contract-month granularity anyway) and the delivery-notices report intermittently 403. `pdfplumber` was pip-installed during this investigation but is unused in shipped code.

**Not yet built**: story #4 (CME firm-level delivery concentration) — the free `MetalsIssuesAndStopsReport.pdf` is fetchable (confirmed live) but CME's bot-blocking makes it unreliable; would need `mc_token.py`-style reverse-engineering effort, not yet attempted.

### Inventory: known gaps

- 📌 Annual market balance series missing 2005–2013 (Silver Institute WSS PDFs located but not parsed) — documented follow-up, not a blocker.
- 📌 `VaultSnapshotPanel`'s pin-to-past-date feature only has real history from whenever `inventory_depository` first started being populated — hovering an older flagged date shows a "no snapshot persisted yet" message; coverage window only grows forward.
- 📌 Gold COMEX history/depositories are fetched+persisted every slow-tier cycle but nothing in `comex_inventory.jsx` renders them yet (only gold leverage has a frontend consumer) — a known no-op, not a bug.

---

## Tab: CATCOR (Catalyst Correlation)

**Frontend**: `frontend/src/catcor_panel.jsx`. **Backend**: `backend/catcor.py`, `seed_data/catcor_events_seed.py`.

### CATCOR: what it does

CATCOR Iteration 1's event/reaction spine — answers "did this catalyst actually move the metal" with observed data only, no interpretation layer. Tracks a macro-event calendar (FOMC, CPI, NFP), sources actual/consensus values from ALFRED/ForexFactory, captures silver/gold price reactions at fixed windows (T-30min/T+5min/T+30min/T+2hr) around each event.

### CATCOR: panel contents

Two Recharts scatter charts sharing a metal (XAG/XAU) toggle:

- **Catalyst Timeline** (top) — every captured catalyst by real calendar date, 1M/3M/6M/1Y lookback (fixed 30-day lookahead). Includes events with no known `surprise_magnitude` yet; known-future events render at reduced opacity as placeholders (never a real 0% reaction).
- **Surprise Magnitude vs. Price Reaction** (below, own T-30m/T+5m/T+30m/T+2h window toggle) — only plots events where a real `surprise_magnitude` exists.

Government-seeded events (FOMC/CPI/NFP) render as diamonds; Research-promoted (`observed`) events render as **circles** in their own amber color — event-type colored via `CATCOR_EVENT_COLORS` either way. Hovering a point in either chart doubles the size of that same event's point in the other (shared `hoveredEventId` state); clicking an `observed` point hotlinks to its originating Research session (see Tab: Research's "Research ↔ CATCOR integration" for the full mechanism — promote/demote, the hotlink wiring, and clickable legend rows all live there since they're driven by the Research tab's disposition flow, not CATCOR's own data).

### CATCOR: data flow

`main.py`'s `lifespan` runs, in order (all best-effort/independently try-excepted): `catcor.seed_events()` (loads `catcor_events_seed.py`'s static FOMC/CPI/NFP list, upserts into `event_calendar` for a 90-day-back/60-day-forward window, `event_id = f"{event_type}_{date}"` so re-seeding is idempotent; each event carries `source_tier`, currently always `"government"`) → `catcor.backfill_intraday_ticks()` + `backfill_daily_closes()` (Yahoo Finance) → `fetch_and_persist_consensus()` (ForexFactory) → `fetch_and_persist_actuals()` (ALFRED) → `backfill_reactions()`.

Three core tables: `event_calendar` (event_id PK), `macro_price_reaction` (PK event_id+metal+window), `spot_price_tick` (PK series_id+ts, **append-only** — fixes a gap `spot_price_snapshot` has: one row per calendar day, overwritten every fast-tier tick, no true intraday series; `_fetch_and_persist_prices` writes to both tables on every fast-tier poll).

**`spot_price_tick`'s series_id families — a real collision, since fixed**: `"XAG"`/`"XAU"` are exclusively `main.py`'s real metalcharts.org spot ticks (60s cadence, power the CoT tab's `PriceHistoryChart`). `"XAG_FUTURES"`/`"XAU_FUTURES"` (`FUTURES_SERIES_ID` in `catcor.py`) are Yahoo SI=F/GC=F futures bars from `backfill_intraday_ticks`, a genuinely different instrument, used only for CATCOR's own event-reaction snapshot capture below. These two used to share the bare `"XAG"`/`"XAU"` keys — futures prints running systematically ~0.3–0.6 higher than spot, interleaved on the same series, produced a real sawtooth artifact in the CoT price chart. Existing historical rows were migrated (re-keyed by a 5-minute-grid timestamp signature — Yahoo's bars land exactly on `:00`-second, no-microsecond, 5-min-multiple timestamps with zero false positives found against the real spot series) rather than discarded.

- **ALFRED actuals**: `_fetch_alfred_change` fetches two consecutive vintage prints for `CPIAUCSL`/`PAYEMS` as of the day after `scheduled_time` and diffs them — a raw ALFRED level is NOT comparable to ForexFactory's consensus (a period-over-period change), a real bug caught during build. NFP's diff is a raw count (thousands of persons); CPI's is % m/m. `surprise_delta = actual - consensus`, recomputed whenever either value is already present regardless of fetch order.
- **Consensus**: `fetch_and_persist_consensus` matches `event_calendar` against ForexFactory's free `ff_calendar_thisweek.json` feed (title + same-instant match — "Non-Farm Employment Change" specifically, not "ADP Non-Farm Employment Change"). Only a "this calendar week" variant exists (nextweek/lastweek both 404) — consensus can only ever be captured within roughly ±6 days of "now," a permanent disclosed gap. Fetched at most once per calendar week, persisted to `forexfactory_calendar` (every entry the feed returns, every country — keyed `(week_key, title, country, event_date)`) — confirmed live that repeat hits trip a 429 and can lock out an IP entirely, so never re-fetched within the same week. This replaced an earlier flat-file cache (`runtime/forexfactory_thisweek.json`) since the file was doing double duty as a second, inconsistent persistence mechanism for data with real long-term value beyond the USD/CPI/NFP subset CATCOR actually matches. `catcor_consensus_actuals`'s `CadenceSpec(trigger="interval", interval_seconds=1800)` in `backend/sources.py` has `_schedule_loop` call it (via `_catcor_consensus_tick`) periodically alongside `fetch_and_persist_actuals`. ForexFactory's `"K"`-suffixed NFP forecasts (e.g. `"114K"`) must be scaled ÷1000 to PAYEMS's native thousands unit — a second real unit bug caught during build.
- **Snapshot capture** (`catcor.capture_snapshot`): idempotent by construction — skips if a `macro_price_reaction` row already exists for `(event_id, metal, window)`. `due_snapshots()` finds boundary-passed-but-uncaptured pairs across **every** `event_calendar` row unconditionally (no `event_type` filter — this is what lets Research-promoted `observed` events get their reactions captured automatically, with zero code changes needed for that), polled every 60s by `_schedule_loop` (via `_catcor_snapshot_tick`) under `catcor_snapshot`'s `CadenceSpec(trigger="always_on", interval_seconds=60)` in `backend/sources.py` — `always_on` is a distinct trigger value from the ordinary `"interval"` sources specifically so this source is structurally incapable of being gated by an enabled flag, since a missed window is permanent data loss unlike re-fetchable tier data. Price lookup: `db.get_ticks_near` finds the nearest `spot_price_tick` row **under `FUTURES_SERIES_ID`'s keys specifically** (`"XAG_FUTURES"`/`"XAU_FUTURES"`, explicit as of the series-collision fix above — CATCOR's reactions were always effectively sourced from Yahoo's futures bars, not real spot ticks, this just makes that explicit rather than accidental); falls back to `backfill_daily_closes`'s dedicated `XAG_DAILY_CLOSE`/`XAU_DAILY_CLOSE` keys (NOT the Money Supply tab's `XAG_CLOSE`/`XAU_CLOSE` keys — those are month-end resampled with no daily granularity, a real bug caught during build). `backfill_intraday_ticks` pulls Yahoo 5-minute bars (capped ~72 days back; 1-minute is capped at only 8 days, too short) — so ~72 of a 90-day backfill window get real intraday-precision reactions, remaining ~18 oldest days fall back to same-day daily-close approximation.
- **Routes**: `GET /api/catcor/events/db`, `GET /api/catcor/reactions/db`, `POST /api/catcor/refresh` (manual re-seed+backfill+re-fetch, `500` if `FRED_API_KEY` unset).

### CATCOR: known gaps

- 📌 `consensus_value` only populates for events within the current Sun-Sat week — events outside it keep `actual_value` but never get `surprise_magnitude`, permanent limitation of the free source, not a bug to chase.
- 📌 Intraday-precision reactions only exist for the ~72 most recent days of the 90-day backfill window.
- 📌 Iterations 2+ beyond the Research tab (FedWatch, standing watchlists, sentiment scoring) not yet built — no dedicated spec file exists yet for this; write one (per-feature convention) if/when this gets picked up.

---

## Tab: Research (CATCOR Research Pane, per `catcor-events-spec.md`)

**Frontend**: `frontend/src/research_panel.jsx`. **Backend**: `backend/catcor_research.py`, `backend/db.py`'s research tables, `backend/prompts/*.py`.

### Research: what it does

A workbench for working a single claim/observation by hand, one turn at a time, ending in a disposition: promote it to a tracked Catalyst Event (renders on the CATCOR timeline, see below), dismiss it as noise, or discard it outright. Supersedes the original "Iteration 2" chat MVP — nothing is auto-fetched by a model deciding it's relevant; every turn is assembled from five independently-adjustable, human-set controls (spec section 3):

1. **Model** — `forge` (amp-forge, a local Ollama-backed LAN service) or `anthropic` (Claude), chosen per turn, `forge` is always the default.
2. **Persona** — dynamically listed from `backend/prompts/*.py` (see Personas below), no registration step.
3. **Context blocks** — six checkboxes (`cot_positioning`, `comex_inventory`, `money_supply`, `market_balance`, `prior_turns`, `freeform`), each folded into the prompt only if explicitly checked.
4. **Memory mode** — `stateless` or `accumulating`; a session's current mode is stored and defaults the toggle to wherever it was left.
5. **Prompt transparency** — a live, non-editable preview of the exact assembled payload (persona text + checked context blocks' real formatted data + draft input) before sending, computed server-side via `POST /sessions/{id}/preview` (calls `assemble_prompt` directly, zero model cost) rather than approximated client-side; each turn's assembled prompt is also persisted so the transcript can show exactly what was sent, after the fact.

### Session lifecycle

Four states: `active` (editable, only status that can send turns) → `promoted` | `dismissed` (both terminal/read-only) → `discarded` (hard-deleted, never a stored status value — `research_sessions`/`research_messages` rows are actually removed, not flagged). Gating rules (`catcor_research.py`):

- **Promote** (`promote_session`) — requires a read (bullish/bearish/neutral) already set. Writes a new `event_calendar` row: `event_type="observed"`, `source_tier="discovered"`, `research_session_id` backlinking to the session, `direction` = whatever was chosen in the promote form. `consensus_value`/`actual_value`/`surprise_delta` stay `NULL` (not applicable to an Observed-origin event).
- **Dismiss** (`dismiss_session`) — requires a read, a non-empty reason, and ≥1 turn already sent (a zero-turn session has nothing to reason about — it can only be discarded). Writes a `research_log` row.
- **Discard** (`discard_session`) — no gating beyond rejecting an already-terminal session (discarding a promoted/dismissed session would orphan the `event_calendar`/`research_log` row it already produced — see Demote below for the actual "undo a promotion" path). Hard-deletes the session and its messages.
- **Demote** (`delete_promoted_event`, new) — the reverse of promote: `DELETE /api/catcor/events/{event_id}` deletes the `event_calendar` row and its `macro_price_reaction` rows, then reverts the originating session back to `active` (editable again — can be re-promoted with a corrected date/name, dismissed, or discarded). Rejects government-seeded events (`source_tier != "discovered"`) — this is only for undoing a Research promotion, never for editing AV's own seeded calendar. Surfaced in the Research tab's **session list** (not the session detail view) as a bulk action: selecting one or more `promoted` rows lights up a ⬇ header button (selecting `active` rows instead lights up a 🗑 discard button; a genuinely mixed active+promoted selection leaves both buttons visibly disabled rather than guessing which action was meant).

### Personas (`backend/prompts/*.py`)

The only contract `catcor_research.list_personas()`/`load_persona_prompt()` enforce is a module-level `PROMPT` string constant — nothing else in a persona file is read by any code (headers/docstrings are human documentation only, following an established-but-unenforced convention). Personas are listed by scanning `backend/prompts/*.py` (excluding `__init__`/`__pycache__`), so a new file is selectable immediately with no registration step. Live personas:

- `word_count_v1` — trivial word-counter, the original plumbing-validation persona, still available but no longer the hardcoded default.
- `parser_v1` — the original combined decompose+match persona, built around a `TOOL_REQUEST:` plain-text protocol for a model-driven tool-use loop. **That loop no longer exists** (superseded by the human-checked context-block design above) — left in place, unwired, importable, but selecting it will produce inert `TOOL_REQUEST:` lines nothing intercepts.
- `parser_v2` — decomposition-only (supersedes `parser_v1`'s combined job, single-responsibility split): claim text in, a numbered list of testable sub-assertions out, each tagged CHECKABLE/NOT CHECKABLE. No tool references, no matching, no verdict.
- `matcher_v1` — matching-only, the other half of `parser_v1`'s old combined job: a list of sub-assertions in, each one paired with which of the four AV data types (`cot_positioning`/`comex_inventory`/`money_supply`/`market_balance` — same names the context-block checkboxes use) would test it, or `none`. Never fetches or invents real data — naming a match is separate from actually reading that data type's current value.
- `parser_v3` — currently identical behavior to `parser_v2`; created as a documented template (every available header/option/variable spelled out, since the only real option is `PROMPT` itself) rather than a behavior change.
- `analyst_v1` — contextualizing/connecting persona from the old 2-call pipeline (takes already-matched evidence, explains historical-range context, never renders a verdict); still selectable standalone now that any persona can run on any turn.

**No persona, on either backend, has real tool-calling access** — `call_anthropic`/`call_forge` are both pure text-in/text-out (no `tools` schema sent to Anthropic's API; amp-forge's payload has no tool-schema field either). This is a deliberate architecture choice, not a Claude-vs-Ollama capability gap: AV data reaches a model exclusively via the context-block mechanism (a human checks a box, Python fetches the real data and pastes formatted text into the prompt **before** the model sees the turn) — no persona ever decides what data it gets.

### Model backend layering

`call_anthropic(system_prompt, messages, model)` / `call_forge(same signature)` — one function per vendor, identical signature and return shape (`{"final_text": str}`), pure text-in/text-out. `call_ai(backend, ...)` is a thin dispatcher between them.

- **Anthropic**: raw httpx POST to `https://api.anthropic.com/v1/messages` (no SDK, same convention as every other outbound integration in this codebase), model `claude-haiku-4-5-20251001` — Haiku specifically, not Sonnet, a deliberate cost choice since this loop is exercised interactively/repeatedly during testing (confirm the exact model string against docs.claude.com if it ever needs correcting). Requires `ANTHROPIC_API_KEY`; has no mock mode — a `mock_key` passed to `call_anthropic` raises loudly rather than silently ignoring it, since this is real, billed traffic. `_require_backend_credentials` checks the *per-request* chosen backend, not just the server-wide default, so an explicit `"anthropic"` choice against a `forge`-default server still gets a clean `500` rather than a raw `KeyError`.
- **amp-forge** (`call_forge`): one POST to a local Ollama-backed LAN service's `/chat/stream` (`FORGE_URL`, default `http://amp-forge:8001/chat/stream`; see `forge-spec.md` in the amp-dev repo for the stateless/system-prompt contract), model `qwen3:8b`, accumulates SSE `token` events. Supports a `mock_key` to skip Ollama and stream back a fixed fixture instead. The `history` field in its payload is what powers Accumulating memory mode.
- **`DEFAULT_BACKEND`**, read from `AI_BACKEND` env var, **defaults to `"forge"`** — Anthropic is an explicit opt-in for real (billed) compute.

### Context assembly (`assemble_prompt`, `catcor_research.py`)

Four of the six context blocks map to the four evidence-tool functions (`_tool_get_cot_positioning`/`_tool_get_comex_inventory`/`_tool_get_money_supply`/`_tool_get_market_balance`, unchanged from the old pipeline, still also backing `GET /api/catcor/research/evidence/db`'s zero-cost debug dump) via a new dict→labeled-text formatting layer (`_fmt_cot_positioning` etc. — this formatting step didn't exist anywhere in the codebase before this rebuild). The other two (`prior_turns`, `freeform`) have no backing tool: `prior_turns` controls whether `messages` includes session history at all, `freeform` folds in caller-supplied ad hoc text.

**The one subtle piece**: prior-turn history is included only if `memory_mode == "accumulating"` **AND** `"prior_turns"` is checked — both, not either. Stateless always wins regardless of the checkbox (spec 3.4); an unchecked box means that data is absent, full stop (spec 3.3).

### Research: data flow

`POST /api/catcor/research/sessions` → `create_session` (persists session row only) → immediately followed by `send_message` with the claim as the first turn. `send_message` now takes the full five-control signature (`backend`, `model`, `persona`, `context_blocks`, `memory_mode`, `freeform_text`, plus `system_prompt_override`/`messages_override` for the prompt-preview's per-section edit bypass) — persists the user turn immediately (with `context_blocks`/`memory_mode`/`memory_changed`/`assembled_prompt` recorded on that row) → persists the assistant turn only if the model call fully resolves (with `backend`/`model`/`persona` recorded on that row). Raises if the session isn't `active` (→ route layer returns `409`).

Routes: `POST /sessions`, `GET /sessions/db`, `GET /sessions/{id}/db` (now also returns `promoted_event` — an `event_id`/`event_name`/`scheduled_time`/`direction` object whenever `status == "promoted"`, via a new `db.get_event_for_session` reverse lookup), `POST /sessions/{id}/messages`, `POST /sessions/{id}/preview`, `POST /sessions/{id}/read`, `POST /sessions/{id}/promote`, `POST /sessions/{id}/dismiss`, `POST /sessions/{id}/discard`, `DELETE /api/catcor/events/{event_id}` (demote), `GET /personas`, `GET /evidence/db`, `GET /forge-sessions` (stub, see below).

Tables: `research_sessions` (+ `memory_mode` column, tracks the session's current setting). `research_messages` (+ `backend`/`model`/`persona` — assistant rows only — and `context_blocks`/`memory_mode`/`memory_changed`/`assembled_prompt` — user rows only; a "turn" is still a user-row/assistant-row pair, fields split across the two roles rather than duplicated). `research_log` (+ `dismiss_reason`, required non-empty). `event_calendar`'s `research_session_id`/`direction` columns are now actually written (by promote) and read (by demote, and by `list_research_sessions`'s `LEFT JOIN` which surfaces `promoted_event_id` per row for the session list's bulk-demote action) — no longer dead columns.

**amp-forge session visibility** (spec 3.4) — a known, disclosed gap: amp-forge may hold its own model-side session/context independent of what AV resends, and viewing/clearing that state has no implemented mechanism. `GET /api/catcor/research/forge-sessions` is a stub returning `{"success": false, "detail": "...not yet available..."}` rather than guessing at a wire contract that lives in a separate repo's `forge-spec.md`.

### Research ↔ CATCOR integration

A promoted session's `event_calendar` row (`event_type="observed"`) renders on the CATCOR timeline exactly like a government-seeded event, with two differences: it's a **circle**, not a diamond (government events keep the diamond shape — `makeLinkedShape`'s `diamond` param is now `type !== "observed"`), and its own palette color (`CATCOR_EVENT_COLORS.observed`, amber). Both `get_upcoming_events` and `get_event_reaction_series` (backing `/api/catcor/events/db` and `/api/catcor/reactions/db`) now select `research_session_id`/`direction`, which didn't used to be exposed at all.

Clicking an `observed` point's dot in either CATCOR chart navigates straight to the Research tab and opens that point's originating session (via `App.jsx`'s `openResearchSessionId` state, threaded down to `CatcorPanel`'s `onOpenResearchSession` and `ResearchPanel`'s `openSessionId` prop) — a government-seeded point has no `research_session_id` and is a no-op on click. Both chart tooltips show a "Click to open the research record →" hint when a point has one.

The CATCOR legend is now click-to-toggle (same convention as the CoT panel's line legend): clicking any event-type row hides/shows that type on both charts simultaneously (`hiddenTypes` state, filtered before grouping into `pointsByType`/`timelineByType`); a hidden type's legend row dims but the legend itself always shows every type regardless of what's currently plotted.

**Note on reaction capture for promoted events**: `capture_snapshot`'s nearest-tick lookup already worked generically for `observed` events with zero code changes — `due_snapshots()` queries `event_calendar` unconditionally, no `event_type` filter — so promoting a session with a recent/past `scheduled_time` gets its 4 reaction windows captured automatically on the next 60s poll, same as any CPI/FOMC/NFP event.

### Research: known gaps

- 📌 amp-forge's own session-list/clear API is unconfirmed/unimplemented (see stub above) — AV's own Stateless/Accumulating toggle is fully real, but whatever amp-forge itself might be holding onto server-side is invisible.
- 📌 `parser_v1`'s `TOOL_REQUEST:` protocol is dead but the persona file/prompt text is unedited — selecting it produces inert sentinel lines nothing parses. Flagged as a low-priority cleanup, not fixed.
- 📌 Long-horizon claim verification (e.g. "this claim implied X oz/yr of demand — did COMEX/CoT data actually show that months later") is explicitly out of scope for this build — `macro_price_reaction`'s 4 fixed short windows around one instant don't fit that use case at all; a future, differently-shaped feature, not a variant of what exists today.

---

## Tab: Data

**Frontend**: `frontend/src/data_editorial.js` (hand-written prose/curl examples, split from the former `data_map.js` per datasources-spec.md Story #1) + `frontend/src/data_panel.jsx`. **Backend**: `backend/sources.py` (canonical operational registry) + `backend/db.py`'s `source_health` table + `main.py`'s `/api/health/*` and `GET /api/data-sources/db` routes.

### Data: what it does

Two things layered together, since datasources-spec.md Story #1's split: a **static, hand-authored editorial map** (`data_editorial.js` — provenance prose, curl examples, per-field descriptions; not introspectable from SQLite at runtime, so this must be kept in sync by hand, see the strict Data-tab-update rule under Standing rules above), joined against **operational metadata read live from `backend/sources.py`'s registry** (`GET /api/data-sources/db` — cadence, rate limit, affinity group, `requires_env`), plus a **live Data Health view** (per dataHealth-spec.md) showing real-time fetch status per source (`GET /api/health/db`).

### Data: panel contents

`DataPanel` (outer, defaults open) fetches both `/api/health/db` (polled, on refresh) and `/api/data-sources/db` (once per mount — operational metadata doesn't change at runtime) → `TieredLoopSummary` (fast/slow tier enabled+interval from `GET /api/refresh/settings`, plus healthy/total rollup per tier from `/api/health/db`'s server-derived `tier` field) → one nested `<details>` per editorial entry (`SourceCard`): origin as a key-value row, then per-`sourceKeys` entry cadence/rate-limit rows (`RateLimitDisplay` — Story #6's real enforced state: a live "next eligible fetch: in N days" countdown for `min_gap_derived` sources, the raw quota for `numeric_quota` sources, an explicit "undocumented — advisory only" badge for reverse-engineered sources, never a fabricated number) and a `FetchStatusRow` (status dot computed client-side — `computeStatus`, exported from `data_panel.jsx` and shared by `App.jsx`'s `HeaderHealthDot` rather than each re-implementing the same ok/stale/error rule — against that source's `expected_interval_s`, now shipped directly on each `/api/health/db` row instead of a separate frontend-hardcoded `expectedIntervalS`; last-success time; truncated last-error text; independent "Re-run now" button hitting `POST /api/health/refresh/{source_key}` — `cot_pipeline`'s button additionally grays out inside its 7-day rate-limit window), the `curl` block in a horizontally-scrollable `<pre><code>`, then a table per SQLite table listing every field + description.

Sources with no periodic fetch of their own (`catcor_calendar`'s static seed, `research`'s on-demand chat) simply omit `sourceKeys` in `data_editorial.js` (and have no corresponding entry in `backend/sources.py`'s registry).

### Data: data flow

`source_health` (source_key PK, last_attempt_at, last_attempt_status [success|error|skipped], last_success_at, last_error, consecutive_failures — upsert, current-state only, not append-only, one row per fetch-and-persist function/loop) is infrastructure describing the health of every other tab's upstream fetch — not itself a market-data table, so it has no `data_editorial.js` SourceCard of its own.

**`backend/sources.py`** (per datasources-spec.md Stories #1+#3) is the canonical registry — replaces what used to be three separate hand-written dicts (`_FAST_TIER_REGISTRY`/`_SLOW_TIER_REGISTRY`/`_ON_DEMAND_REGISTRY`, merged into `_SOURCE_REGISTRY`) plus a module-level `_SELF_RECORDING_KEYS` set and three bespoke per-source rate-limit `if` blocks. Every source is one `SourceDefinition` (key, label, `affinity_group`, `fetch_fn`, `tables`, `cadence: CadenceSpec`, `rate_limit: RateLimitSpec`, `requires_env`, `self_recording`), registered into `sources.SOURCE_REGISTRY` from `main.py` once every fetch function it references exists as a real name (same ordering constraint the old registries had). `CadenceSpec.trigger` is one of `"interval"` (fired by the generic scheduler on a fixed cadence, subject to `enabled_flag`), `"always_on"` (fired unconditionally, no `enabled_flag` branch ever consulted — CATCOR's `catcor_snapshot` reaction-capture source uses this specifically because a missed window is permanent data loss, unlike every re-fetchable source), `"manual_only"` (reachable only via `POST /api/health/refresh/{source_key}`), or `"startup"` (fired once at boot from `lifespan`, outside the generic scheduler — `_catcor_startup`/`_lbma_fix_startup`/`_census_trade_startup`'s internal ordering is load-bearing and stays hand-written, not folded into `sources.py`'s dispatch). A single `_schedule_loop()` in `main.py` (ticking every 1s, dispatching per-source on its own `CadenceSpec`) replaces the old `_fast_tier_loop`/`_slow_tier_loop`/`_event_tier_loop`/`_consensus_tier_loop` four-loops-by-era pattern — including CATCOR's `catcor_snapshot`/`catcor_consensus_actuals`, which were never in any registry before this pass. `POST /api/refresh/force` and the one-shot startup refresh still call `_refresh_fast_tier`/`_refresh_slow_tier` directly, now reading `sources.sources_by_tier("fast"/"slow")` instead of the old dicts, so they can't drift from what `_schedule_loop` itself fires.

`GET /api/health/db` remains a thin read of `source_health` plus two fields derived at read time from the matching `SourceDefinition`, never stored: `expected_interval_s` (from `CadenceSpec.expected_interval_s`, the property `2 × this` staleness threshold is computed against) and `tier` (a small scheduling-only label — `"fast"|"slow"|"always-on"|"interval"|"startup"|"on-demand"`, deliberately distinct from `affinity_group`'s four-value source-category grouping, so `TieredLoopSummary`'s existing fast/slow rollup keeps working unchanged). The route still does not compute ok/stale/error itself — the frontend computes status client-side against the shipped `expected_interval_s`.

A small always-visible header dot (`HeaderHealthDot` in `App.jsx`) polls `/api/health/db` every 60s independent of which tab is active — red if any tracked source is erroring, yellow if any is stale with no errors, green otherwise; links nowhere, since the Data tab nav button is already one click away for drill-down.

### Census International Trade (`census_trade`, per international-trade-spec.md)

The one data source that lives in this tab and nowhere else — no frontend panel exists for it yet (v1 is backend + DB + Data tab only, per the spec's explicit scope). U.S. Census Bureau International Trade API, monthly imports/exports for HS 7106 (silver) and HS 7108 (gold, comparison-only per the standing "gold as context" rule — no gold-specific signal or panel). Answers "where does US silver supply actually come from, and is that mix shifting" with a primary-source view of trade flow (value, quantity, country-of-origin/destination) — does not attempt to reproduce USGS's net-import-reliance %, which needs domestic mine-production data AV doesn't track.

`census_trade` table (`metal`, `flow`, `hs_code`, `cty_code`, `cty_name`, `year`, `month`, `value_general_usd`, `value_consumption_usd`, `qty`, `qty_unit`, `fetched_at`; PK `(metal, flow, hs_code, cty_code, year, month)`) — **upsert, not append-only** (`INSERT ... ON CONFLICT DO UPDATE`, same shape as `lbma_fix`), since Census revises trade data annually every April, unlike CFTC's immutable-once-published reports.

**Confirmed live** (real `CENSUS_API_KEY`, both flows, both metals, several months, including a real production run): field names are `GEN_VAL_MO`/`CON_VAL_MO` (imports) and `ALL_VAL_MO` (exports) for value, `GEN_QY1_MO`/`CON_QY1_MO` (imports) vs. the differently-named `QTY_1_MO` (exports) for quantity, sharing `UNIT_QY1` for the unit code. **Census reports no quantity/weight at all for HS 7106 or HS 7108** — the qty fields are always `"0"` and `UNIT_QY1` is always `"-"` (Census's own not-applicable sentinel), consistently across every row checked. `qty`/`qty_unit` persist as `NULL` accordingly; this is a schema-level upstream gap, not a per-row data hole, and not something to build oz-conversion logic against.

**Also confirmed live, a real correction mid-build**: Census's true publication lag is **~2 months, not ~1** — both the current calendar month and the immediately-prior one return HTTP `204` (empty body, not an error status) until released. `_fetch_and_persist_census_trade` treats `204`/empty-body responses as "not yet published, skip this month" and fetches a wider window (`CENSUS_TRADE_MONTHS_PER_FETCH + 2` months back) so 3 real months still land per run. This lag also broke the originally-spec'd rate-limit gate design: gating on "is the latest **persisted** `(year, month)` under 25 days old" (mirroring `_refresh_cot_pipeline`'s CFTC pattern, where report age closely tracks fetch recency) is never true in practice for Census, since by the time a new month is available the previous latest month is already 2+ months old — the gate would never actually skip. `_refresh_census_trade` instead gates on **wall-clock time since the last fetch attempt** (`source_health.last_attempt_at`, via a new `db.get_source_health(source_key)` single-row read), which correctly approximates "don't re-check Census more than once per ~25 days" regardless of publication lag.

`_refresh_census_trade` (`main.py`) wraps `_fetch_and_persist_census_trade` with that gate — skips (records `last_attempt_status="skipped"`) if the last fetch attempt is under ~25 days old, otherwise fetches and records its own success, since its `SourceDefinition.self_recording=True` (alongside `cot_pipeline`) in `backend/sources.py`. Cadence: `CadenceSpec(trigger="startup", min_gap=timedelta(days=25), gate_on="last_attempt_at")` — gated startup fetch + manual force-refresh via the Data tab's "Re-run now" button — silently skips at startup if `CENSUS_API_KEY` isn't set, same as LBMA. `GET /api/census-trade/db?metal=&flow=&hs_code=` is the sole read route — no live `/refresh` route, per the same "a government monthly release isn't something a page load should trigger" reasoning as CoT. `GET /api/health/db` enriches `census_trade`'s row with `last_period` (via `db.get_latest_census_trade_period()`), same convention as `cot_pipeline`'s `last_report_date`.

### Data: known gaps

- 📌 `census_trade`'s `qty`/`qty_unit` are confirmed-live always `NULL` — Census does not report a quantity/weight figure for HS 7106 or HS 7108 today. Not a bug; revisit only if Census ever starts populating `UNIT_QY1` for these codes.
- 📌 `census_trade` has no frontend panel — v1 is backend + DB + this Data tab entry only, per international-trade-spec.md's explicit scope. HS 7112 (scrap) and other Critical Minerals List comparators (e.g. copper 7402/7403) are deferred to a documented Phase 2, not discarded.

This tab's own gaps are otherwise, by design, whatever every other tab's Known gaps sections say about their upstream fetches.

---

## Repo layout

```text
backend/
  main.py               FastAPI app: fetch+persist functions + /db read routes for every tab above, plus /api/refresh/settings + /api/refresh/force (tiered refresh control) and /api/health/* (Data Health). Serves frontend/dist. Invoked as `uvicorn backend.main:app` (package-qualified, not cwd-relative).
  db.py                 SQLite persistence (sqlite3 stdlib, no ORM) — runtime/argentvigil.db, DB_PATH computed __file__-relative, os.makedirs guard. See each tab section above for its own tables; gold has parallel tables to silver (gold_inventory_aggregate, gold_inventory_depository, gold_volume_oi) throughout.
  mc_token.py            metalcharts.org auth token fetch/cache (module-level globals, single-process only)
  sources.py             Canonical data-source registry (datasources-spec.md Stories #1+#3) — SourceDefinition/CadenceSpec/RateLimitSpec, populated from main.py into sources.SOURCE_REGISTRY. Single source of truth for cadence/rate-limit/self-recording that main.py's _schedule_loop, POST /api/health/refresh/{source_key}, and GET /api/health/db (Tab: Data above) all read from.
  catcor.py               CATCOR tab backend (see Tab: CATCOR above)
  catcor_research.py      Research tab backend (see Tab: Research above)
  delivery_behavior.py    Delivery Behavior cross-check layer, feeds both Inventory and CoT tabs (see those sections above)
  prompts/                Research tab's model personas — all live/selectable (see Tab: Research's Personas subsection): word_count_v1.py, parser_v1.py (dead TOOL_REQUEST: protocol, unwired but importable), parser_v2.py, parser_v3.py, matcher_v1.py, analyst_v1.py. Only contract: a module-level PROMPT string.
  __init__.py             Empty — makes backend/ a real package

seed_data/
  catcor_events_seed.py        Manually-maintained FOMC/CPI/NFP calendar (dates cross-checked against ALFRED + the Fed's own meeting calendar). Each event carries source_tier (currently "government" for all three types).
  silver_market_balance.json   Manually maintained annual Silver Institute balance data (Inventory tab's Market Demand section)
  cme/112.pdf, cme/113.pdf     COMEX rulebook Chapters 112 (Silver)/113 (Gold) — reference only, confirms Delivery Behavior's Last Trade Day rule

runtime/            Gitignored — generated state, not source. Just argentvigil.db.

pipeline/
  config.py         All tunable thresholds/constants for the CoT tab
  fetch.py          CFTC Socrata API (urllib, stdlib only) — both Legacy and Disaggregated datasets
  price_fetch.py    Yahoo Finance chart API (urllib, stdlib only) — CoT tab's price series only
  compute.py        net_long_pct_oi, percentile ranks, signal classification, track record (CoT tab)
  run.py            Entry point — fetch -> compute -> persist -> cache/cot_data.json
  cache/cot_data.json   Pipeline output (gitignored) — standalone-CLI usability only, not read by the frontend

frontend/
  src/App.jsx                 Top-level composition, owns activeSection tab state (SECTIONS: cot, moneySupply, inventory, catcor, research, data). All six sections mounted unconditionally with a section-hidden class toggled by activeSection — never conditionally rendered/unmounted, so switching tabs never refires mount-time fetches/listeners. Also owns pinnedSection (see Standing rules' pinned-default-tab entry) and openResearchSessionId (CATCOR-dot-click-to-Research-session hotlink state, threaded to CatcorPanel/ResearchPanel — see Tab: Research's integration subsection).
  src/silver_cot_tracker.jsx  CoT tab (see Tab: CoT above)
  src/comex_inventory.jsx     Inventory tab (see Tab: Inventory above)
  src/delivery_behavior_panel.jsx  Delivery Behavior panel, nested in comex_inventory.jsx (see Tab: Inventory above)
  src/market_balance.jsx      Market Demand / Demand Composition, nested in comex_inventory.jsx (see Tab: Inventory above)
  src/money_supply.jsx        Money Supply tab (see Tab: Money Supply above)
  src/catcor_panel.jsx        CATCOR tab (see Tab: CATCOR above)
  src/research_panel.jsx      Research tab (see Tab: Research above)
  src/data_editorial.js        Data tab's hand-written editorial map (prose/curl/field descriptions) -- split from data_map.js per datasources-spec.md Story #1; operational metadata (cadence/rate-limit) now lives in backend/sources.py (see Tab: Data above)
  src/data_panel.jsx           Data tab's rendering -- joins data_editorial.js against GET /api/data-sources/db + GET /api/health/db (see Tab: Data above)
  src/refresh_controls.jsx     FORCE_REFRESH_EVENT export only — the RefreshControls UI component itself is no longer rendered anywhere (its "Configuration" collapsible was removed at the user's request); file kept because 4 other files still import the event constant. Backend refresh-tier settings/force endpoints are unaffected, just no UI control for them.
  src/palette.js              Shared chart color constants (VAULT_COLORS, CATCOR_EVENT_COLORS)
  docs/UI_STANDARDS.md         Cross-cutting interactive-UI conventions (legends, tooltips, color coding, sizing) — checked in, not gitignored; see Conventions below
  vite.config.js              publicDir points at pipeline/cache (legacy); /api proxied to :8000

utils/
  vigil.sh            Process manager for backend/frontend as background daemons (start/stop/restart/status, PID-tracked, logs to runtime/logs/) — the standing way to run/stop/restart services, always allowed to run (see Development Notes). Backend picks up Python edits via `vigil.sh restart backend` (no --reload); frontend gets Vite HMR automatically either way.
  dev.sh              Legacy foreground runner (Ctrl-C to stop, uvicorn --reload) — superseded by vigil.sh for a single-user local app; kept only if you want auto-restart-on-save for the backend without typing `vigil.sh restart`.
  sniff-metal-charts.py, sniffer.sh   Tools for reverse-engineering metalcharts.org API responses
  gen_data_dictionary.py   Generates docs/data-dictionary.md from backend/db.py's DDL + backend/sources.py's registry + frontend/src/data_editorial.js's prose (datasources-spec.md Story #2). Requires the venv (imports backend.main to populate SOURCE_REGISTRY), not stdlib-only like pipeline/.
  gen_source_scaffold.py   Deterministic scaffold generator for onboarding a new data source (datasources-spec.md Story #4) — takes a YAML file (see example_source.yaml), emits reviewable SourceDefinition/fetch-function/DDL/route/data_editorial.js boilerplate. Never writes files directly — prints for manual review/apply, per this repo's Learning Mode standing rule. Paired with `.claude/commands/onboard-source.md`, which walks the judgment half (real response shape, quirks, cadence) that produces the YAML this script consumes.
  example_source.yaml      Worked example input for gen_source_scaffold.py.
```

**Convention:** runnable scripts live in `utils/`, not the repo root.

## Conventions

- **Backend**: stdlib `sqlite3` (no ORM), `httpx.AsyncClient` for outbound calls (one shared client via FastAPI lifespan), routes are thin — fetch, reshape, upsert, return `{"success": bool, "data": ...}`. HTTP errors from upstream become `HTTPException(502, str(e))`.
- **Pipeline**: intentionally stdlib-only, no third-party deps — keep it that way. Config constants belong in `pipeline/config.py`, not scattered inline.
- **Frontend**: React 19 + Vite 5 + Recharts, no state library, no router (single page, tab-like sections via `App.jsx`). Chart colors come from `palette.js`, not ad hoc hex values.
- **Units**: COMEX figures are troy oz; SHFE is kg natively, converted via `1 kg = 32.1507 oz` (constant appears in both `main.py` and any frontend display — keep consistent). COMEX open interest contracts are 5,000 oz each.
- **AV Voice Rules** (standing rule, applies repo-wide to any user-facing copy — formerly documented in the retired `SPEC.MD`, now stated here as the canonical source): no "alarming"/"critical" framing on deficit trends, always show the ±20% uncertainty qualifier on above-ground stock figures, label runway/track-record figures explicitly as historical description or context — never as prediction.
- **Collapsible sections**: the established UI pattern for any collapsible pane is native `<details className="collapsible-pane"><summary className="collapsible-pane-title">…</summary><div className="collapsible-pane-body">…</div></details>` (CSS in `index.css`) — nest freely. Prefer this over a `useState` show/hide toggle unless more complex interaction state is needed.
- **Custom legends over Recharts' built-in `<Legend>`**: chart legends in this codebase are hand-rolled (`.comex-legend-list`/`.comex-legend-item`/`.comex-legend-swatch` classes). See `frontend/docs/UI_STANDARDS.md` for the fuller standard this has converged on (horizontal layout, click-not-hover for detail, clicking a legend row must also highlight that series on the chart, always show every relevant item regardless of current value) — check new interactive legends/tooltips against that doc rather than inventing a new pattern per chart.
- **Custom scatter-point shapes for cross-chart consistency**: CATCOR's two charts and its legend all use the same diamond shape per event type (`.comex-legend-swatch--diamond` for the legend; a shared `makeLinkedShape` renderer for the charts themselves).

## Running it

```bash
bash utils/vigil.sh start         # everything, as background daemons (venv bootstrap + backend :8000 + frontend :5173)
bash utils/vigil.sh restart backend   # after backend Python edits — picks up the change (no --reload running)
bash utils/vigil.sh stop          # stop everything
python3 pipeline/run.py           # CoT pipeline only, no server needed
```

Run the pipeline at least once before the frontend, since the CoT tab reads from `/api/cot/db`, which reads `cot_silver`/`cot_gold` from `runtime/argentvigil.db` — empty tables make that route return a `500` ("No CoT data persisted yet. Run pipeline/run.py first.").

The exchange-inventory/FRED/spot-price data populates itself on first backend startup regardless (a one-time unconditional refresh runs in `main.py`'s `lifespan`) — the tiered background refresh only *repeats* on a schedule if explicitly enabled via `POST /api/refresh/settings` or forced via `POST /api/refresh/force`. CATCOR's event calendar and reaction backfill also run automatically on every startup — no manual trigger needed, though `/api/catcor/refresh` exists for an on-demand re-run. ALFRED calls need `FRED_API_KEY` set in whatever shell launches the backend — without it, the event calendar and price reactions still populate, but `actual_value`/`surprise_delta` stay `NULL`. Research's Anthropic backend needs `ANTHROPIC_API_KEY` only if `AI_BACKEND=anthropic` is explicitly set (default is `forge`, which needs no key but does need the `amp-forge` LAN service reachable). The LBMA fix (see Silver/Gold sections above) needs `GAPI_API_KEY` (GoldAPI.io, free tier) — without it, `_lbma_fix_startup` logs a skip message and the rest of the app boots normally; `lbma_fix` just stays empty and `LbmaFixBadge` renders nothing.

**Always run Python through `.venv`, never bare `python3`.** `bash utils/vigil.sh start` creates
`.venv` on first run and installs `requirements.txt` into it (`fastapi`, `uvicorn`, `httpx`,
`python-dotenv` — none of these are on system Python). Before running any Python command
against this repo — import checks, one-off scripts, `python -c "..."` sanity tests — run
`source .venv/bin/activate` first, or invoke `.venv/bin/python` directly. `pipeline/` is the one
exception: it's intentionally stdlib-only by design and *can* run under bare `python3`, but
`main.py`/`db.py`/anything importing `fastapi`, `httpx`, or `dotenv` cannot.

## Development Notes

Standing instructions for every interaction in this repo — distinct from each tab's "known gaps" (which describe upstream/data limitations, not workflow). Add to this section directly as new quirks/preferences come up.

- **Always run Python through `.venv`, never bare `python3`** — see the full rule under "Running it" above. `pipeline/` is the sole stdlib-only exception.
- **Use `utils/vigil.sh` to stop/start services** — always allowed, no need to ask first.
- **Bump the version number in this file's title (`# ArgentVigil vX.Y.Z`) on every feature completion** — a new spec, or any change bigger than a typo/doc tweak. Not on every edit; only when a development effort actually completes.

### TODO

- 📌 (none yet — add items here as they come up)

## Coding Modes

Two modes govern how work in this repo is done. **Learning mode is the default** — only switch to auto mode when explicitly told to.

### Learning mode (default)

- **Never validate UI changes yourself** — no headless browser, no automated screenshotting. The user is at the screen and will look; acknowledge their eyes are better than a headless check for this.
- **Pause after delivering any "demo-able" change** for an interactive walkthrough — explain/demo it, be ready to defend the approach or change it. The user always makes the final call.
- **The goal is learning** — everything AV touches, from physical vault volumes to how the AI pieces work, not just shipping the change.
- **Ask rather than guess** when something is ambiguous.

### Auto mode (explicit opt-in only)

- **Maximize work done before stopping.**
- **Ask no questions** — if a decision is unavoidable, pick the option that would have been recommended anyway.
- **Guess rather than ask.**

## Data sources (see README.md for full table)

CFTC PRE Socrata API (CoT — both Legacy `jun7-fc8e` and Disaggregated `72hh-3qpy` datasets), Yahoo Finance (SLV/GLD/GC=F/SI=F), metalcharts.org (COMEX/SHFE inventory, volume/OI, delivery notices, spot prices — reverse-engineered API, auth via `mc_token.py`), Sprott direct API (PSLV), Silver Institute World Silver Survey (manually transcribed, annual), ALFRED (FRED's point-in-time vintage API — CATCOR's `actual_value`s), ForexFactory (`nfs.faireconomy.media`'s free calendar export — CATCOR's `consensus_value`s, current-calendar-week only), CME Group COMEX rulebook (Chapters 112/113 — Delivery Behavior's Last Trade Day rule, `seed_data/cme/`), Anthropic Messages API / amp-forge LAN service (Research tab's chat backend), U.S. Census Bureau International Trade API (`census_trade` — HS 7106 silver / HS 7108 gold monthly imports/exports by country, free/CC0, `CENSUS_API_KEY`). CME's Market Data Platform (per-contract-month OI/settlement, the `get_settlements` API) was investigated and confirmed **paid**, not a free source — not integrated.
