# ArgentVigil

Silver speculative-positioning monitor with gold as comparative context. Framing is **"selling dollars, not buying metals"** — is the speculative futures crowd genuinely capitulated, or just pulling back. Not a trading system: no price targets, no prediction framing, no risk-tolerance commentary. See `SPEC.MD` (gitignored, local-only — do not assume it exists in fresh clones) for the full Stock & Flow panel spec and CATCOR feature map, `deliveryBehavior-spec.md` (also gitignored, local-only) for the Delivery Behavior layer's full spec and story list, and `README.md` for the user-facing feature/data-source overview.

## Architecture

Three independent layers that share one frontend. All follow **persist-on-fetch**: every upstream fetch is persisted to SQLite before anything is returned, and the frontend only ever reads from local `/db`-suffixed (or otherwise local-read) endpoints — see `SPEC.MD`'s Persist-on-Fetch Re-architecture for the full rationale (CATCOR's cross-check features assume AV's database is already the real record, not a pass-through).

1. **CoT pipeline** (`pipeline/`) — stdlib-only Python, no server. Fetches CFTC positioning data + Yahoo Finance prices, computes signals, persists an append-only archive to the shared `runtime/argentvigil.db` (via `from backend import db` — see note below), and also writes a static JSON cache (kept for standalone-CLI usability). Run manually/on a schedule, not on every request — `backend/main.py` never triggers this fetch itself. `pipeline/`'s own fetch/compute logic (`fetch.py`, `price_fetch.py`, `compute.py`) stays stdlib-only and requires no third-party packages — but it now shares `backend/db.py` for persistence rather than owning a separate SQLite file, since `backend/db.py` is itself pure `sqlite3`/`os`/`contextlib` (no `fastapi`/`httpx`) and importing it doesn't require the venv. `pipeline/run.py`'s `sys.path` bootstrap adds both `pipeline/`'s own directory and repo root so `from backend import db` resolves regardless of whether it's invoked as `python3 pipeline/run.py` (from repo root) or `python3 run.py` (from inside `pipeline/`).
2. **Exchange inventory backend** (`backend/main.py`, `backend/db.py`, `backend/mc_token.py`) — FastAPI server. Every route that talks to metalcharts.org/Sprott/FRED/Yahoo follows a fetch-and-persist function paired with a `/db` read route; a tiered background refresh (see below) keeps SQLite warm without the frontend ever calling upstream directly. Serves the built frontend as static files.
3. **CATCOR** (`backend/catcor.py`, `seed_data/catcor_events_seed.py`) — CATCOR Iteration 1's event/reaction spine, imported by `backend/main.py` the same way `backend/db.py`/`backend/mc_token.py` are (functions take an `httpx.AsyncClient` explicitly, no owned global client state). Seeds a static macro-event calendar, fetches ALFRED point-in-time actuals + ForexFactory consensus, and captures silver/gold price reactions at fixed windows around each event. See CATCOR subsection under Data flow below for the full mechanism.
4. **Delivery Behavior** (`backend/delivery_behavior.py`) — a cross-check layer sitting on top of data the other three layers already persist (registered/eligible inventory, delivery notices, disaggregated CoT), computing derived anomaly signals rather than owning any new upstream fetch of its own except one: disaggregated CoT (see below). Imported by `main.py` the same way `catcor`/`db`/`mc_token` are. See Data flow's Delivery Behavior subsection for the full mechanism and Feature pipeline state below for what's built vs. permanently out of scope.

`backend/`, `seed_data/`, and `runtime/` split three previously-flat-at-root concerns: app code (a real Python package, `backend/__init__.py`), manually-maintained static seed content, and gitignored generated state (the SQLite DB, CATCOR's ForexFactory cache) — the last two are not a package (no `__init__.py` in either; `seed_data/` is importable as an implicit namespace package the same way `pipeline/` already is, since uvicorn is invoked from repo root as `backend.main:app`, putting repo root on `sys.path`). Every path that used to be a bare cwd-relative string (`db.py`'s `DB_PATH`, `main.py`'s `MARKET_BALANCE_PATH` and its `frontend/dist` static mount) is now computed `__file__`-relative from each module's own location — the previous bare-relative versions only worked because `utils/dev.sh` happened to `cd` to repo root first, a latent portability bug fixed as part of this reorg.

```
backend/
  main.py           FastAPI app: fetch+persist functions + /db read routes for COMEX/SHFE/PSLV/depositories/leverage/delivery/prices (silver + gold), FRED money-supply routes, metals-price routes, market-balance endpoint, /api/cot/db (reads pipeline's SQLite, computes signals server-side), /api/refresh/settings + /api/refresh/force (tiered background refresh control), /api/catcor/* routes, /api/delivery-behavior/db (see delivery_behavior.py below), /api/health/db + /api/health/refresh/{source_key} (Data Health, see below), serves frontend/dist. Invoked as `uvicorn backend.main:app` (package-qualified, not cwd-relative). Per dataHealth-spec.md: `_FAST_TIER_REGISTRY`/`_SLOW_TIER_REGISTRY`/`_ON_DEMAND_REGISTRY` (merged into `_SOURCE_REGISTRY`) replace what used to be a hardcoded tuple literal inside `_refresh_slow_tier` — both the tiered loops and `POST /api/health/refresh/{source_key}` read from the same registry, so the two paths can't drift apart. `cot_pipeline`'s registry entry (`_refresh_cot_pipeline`) is a rate-limit gate (skips entirely, recording `last_attempt_status="skipped"`, if the latest persisted CoT report is under 7 days old) wrapping `asyncio.to_thread(pipeline_run.run_pipeline_once)` — this is also the first in-app trigger CoT has ever had; it was previously cron/manual-`python3`-only with no route at all. `GET /api/health/db` is a thin read of `source_health` (see db.py below) — it does not compute ok/stale/error itself, since the per-source cadence threshold (`expectedIntervalS`) lives in the frontend's `data_map.js`, and duplicating it server-side would create two sources of truth for the same number; the frontend computes status client-side against that value.
  db.py             SQLite persistence (sqlite3 stdlib, no ORM) — runtime/argentvigil.db, DB_PATH computed __file__-relative (two directories up from backend/db.py, into runtime/), with an os.makedirs guard since sqlite3 doesn't create parent dirs. Gold has parallel tables to silver (gold_inventory_aggregate, gold_inventory_depository, gold_volume_oi); fred_observations is a generic (series_id, date, value) store also used for metal price history (XAG_CLOSE/XAU_CLOSE keys — and CATCOR's own XAG_DAILY_CLOSE/XAU_DAILY_CLOSE keys, see below), not FRED-exclusive despite the name. Newer tables added for persist-on-fetch: delivery_notices, shfe_warehouse, pslv_snapshot, spot_price_snapshot (all upsert — no restatement/immutability need identified for these sources). One flagged-but-deferred exception: inventory_depository/gold_inventory_depository carry prev_registered/prev_eligible/prev_total fields, suggesting metalcharts.org may sometimes restate depository figures — a candidate for an append-only upgrade, deliberately not acted on in this migration (would need a schema/compat decision on a live table). CATCOR tables: event_calendar, macro_price_reaction, spot_price_tick, forexfactory_calendar (week_key, title, country, event_date, impact, forecast, previous — append-only INSERT OR IGNORE keyed by (week_key, title, country, event_date); replaced a previous flat-file cache at runtime/forexfactory_thisweek.json, see CATCOR subsection under Data flow for why). CoT pipeline tables (merged in from pipeline/'s previously-separate database, see "Where the databases live" below): cot_silver, cot_gold (both append-only, INSERT OR IGNORE keyed by report_date), cot_prices (ticker+date upsert), pipeline_runs (single-row, id=1, stamped by pipeline/run.py on every completed run — powers the CoT staleness banner's "pipeline run" timestamp independent of whether that run inserted any new CoT rows). Delivery Behavior table: cot_disaggregated (report_date, metal, category, long, short, spreading, open_interest — append-only INSERT OR IGNORE keyed by (report_date, metal, category), same never-overwrite convention as cot_silver/cot_gold; see delivery_behavior.py below). Data Health table (see dataHealth-spec.md, gitignored/local-only like SPEC.MD): source_health (source_key PK, last_attempt_at, last_attempt_status ['success'|'error'|'skipped'], last_success_at, last_error, consecutive_failures — upsert, current-state only, not append-only; one row per fetch-and-persist function/loop, keyed the same as data_map.js's per-source sourceKeys) — infrastructure describing the health of every other table's upstream fetch, not itself a market-data table, so it has no data_map.js SourceCard of its own
  mc_token.py       metalcharts.org auth token fetch/cache (module-level globals, single-process only)
  catcor.py         CATCOR backend module — ALFRED actuals, ForexFactory consensus (persisted per-calendar-week to db.forexfactory_calendar, see below — not a disk file; every source AV persists lives in the one shared SQLite database), Yahoo intraday/daily-close backfill, idempotent snapshot capture. Imports seed_data via `from seed_data import catcor_events_seed as seed` (namespace-package sibling import, see note above). See Data flow's CATCOR subsection for the full mechanism.
  delivery_behavior.py  Delivery Behavior cross-check layer (see deliveryBehavior-spec.md, gitignored/local-only like SPEC.MD). `compute_reclassification_signal` — day-over-day registered-inventory deltas (inventory_aggregate) vs. same-day delivery-notice volume (delivery_notices, `type="ytd"` not `"mtd"` — confirmed live against metalcharts.org that ytd returns ~85 days of history vs. mtd's handful of days-in-month, at no extra fetch cost; silver-only, since delivery_notices/inventory_aggregate have no gold equivalent). `compute_category_composition` — disaggregated CoT category share-of-long-OI over time (both metals), reading a new `cot_disaggregated` table fetched via pipeline/fetch.py's `fetch_disaggregated_cot_data`/`fetch_gold_disaggregated_cot_data` (CFTC's Disaggregated Futures-Only Socrata dataset, `72hh-3qpy` — a different dataset ID from Legacy's `jun7-fc8e`, confirmed via CFTC's own catalog before building; both silver `084691` and gold `088691` codes work against it, same codes as Legacy). Also holds a First Notice Day / Last Trade Day rule engine (`first_notice_day`, `last_trade_day`, `last_business_day`, `days_to_fnd`) computed on the fly, no seed table — see Feature pipeline state below for why FND's actual rule required a real-world correction mid-build, and why contract-month OI (which would make the FND cross-reference precise rather than approximate) is permanently out of scope, not deferred.
  __init__.py       Empty — makes backend/ a real package so main.py/catcor.py can use relative imports (`from . import db`) and so uvicorn can be invoked as `backend.main:app`

seed_data/
  catcor_events_seed.py       Manually-maintained FOMC/CPI/NFP calendar (dates cross-checked against ALFRED's release/dates endpoint and the Fed's own meeting calendar) — same manual-data convention as silver_market_balance.json. Each event also carries a source_tier field (currently "government" for all three types) per SPEC.MD's Anchor Points principle, laying groundwork for future non-government catalyst types (news, social media) without a schema change.
  silver_market_balance.json  Manually maintained annual Silver Institute balance data (Layer 3 of SPEC.MD). Read by backend/main.py via a __file__-relative MARKET_BALANCE_PATH (repo root, computed from backend/main.py's own location, then into seed_data/)
  cme/112.pdf, cme/113.pdf    COMEX rulebook Chapters 112 (Silver Futures) and 113 (Gold Futures), saved for reference — used to confirm delivery_behavior.py's Last Trade Day rule (third-last business day of the delivery month, identical for both metals) directly against the primary source. Not imported/parsed by any code — reference material only, kept because CME's rulebook pages are inconsistently reachable (see Data flow's Delivery Behavior subsection)

runtime/            Gitignored — generated state, not source. Just argentvigil.db (the one shared SQLite database) — CATCOR's ForexFactory feed used to be cached to a separate flat file here (forexfactory_thisweek.json) but that was replaced with a real table (forexfactory_calendar, see backend/db.py above) since the file cache was doing double duty as a second, inconsistent persistence mechanism for data with genuine long-term value (the full weekly economic calendar, not just the few entries CATCOR currently matches against)

pipeline/
  config.py         All tunable thresholds/constants — edit here, not inline. Includes FRED series/window constants, metal-price fetch constants (METAL_PRICE_FETCH_YEARS, XAG_TICKER/XAU_TICKER, XAG_SERIES_ID/XAU_SERIES_ID), CFTC_API_BASE (Legacy, jun7-fc8e) and CFTC_DISAGGREGATED_API_BASE (Disaggregated, 72hh-3qpy — same SILVER_CONTRACT_CODE/GOLD_CONTRACT_CODE work against both datasets). No longer holds a DB path constant — persistence goes through backend/db.py's own DB_PATH now (see below)
  fetch.py          CFTC Socrata API (urllib, stdlib only). `_fetch_cot_for_contract` takes an explicit `api_base` param (Legacy or Disaggregated) so the same pagination/cutoff-date logic serves both `fetch_cot_data`/`fetch_gold_cot_data` (Legacy) and `fetch_disaggregated_cot_data`/`fetch_gold_disaggregated_cot_data` (Disaggregated) — no duplicated fetch logic between the two report types
  price_fetch.py     Yahoo Finance chart API (urllib, stdlib only) — used only by the CoT pipeline; main.py has its own separate httpx-based Yahoo fetch for the metals-vs-purchasing-power feature (different interval/resampling needs, not a shared dependency)
  compute.py        net_long_pct_oi, percentile ranks, signal classification, track record. `parse_and_compute` takes raw CFTC rows; `compute_from_series` is an additive twin that takes already-normalized rows (i.e. read back from SQLite) and reuses the same percentile/window logic — added for /api/cot/db, does not change parse_and_compute's behavior. Pipeline-internal imports (`from config import ...`) use a try/except package-or-bare-script fallback so the same file works whether run standalone (`python3 pipeline/run.py`) or imported as a package. Pure computation, no database awareness — untouched by the CoT/backend DB merge. Not used for disaggregated CoT — `pipeline/run.py`'s `_disaggregated_rows_for_db` reshapes those raw rows directly, since the disaggregated signal (category composition, in delivery_behavior.py) is a different computation from Legacy's percentile/track-record logic
  run.py             Entry point — orchestrates fetch -> compute -> persist to the shared runtime/argentvigil.db (via `from backend import db`, see Architecture above) -> cache/cot_data.json (unchanged, kept for standalone use). No longer owns a separate pipeline-only database. Also fetches+persists Disaggregated CoT for both metals (`_disaggregated_rows_for_db` fans one CFTC report row out into 4 category rows — producer_merchant/swap_dealer/managed_money/other_reportable — per report_date; producer_merchant has no spread field in CFTC's schema, so its `spreading` is always `None` by design, not a data gap)
  cache/cot_data.json   Pipeline output (gitignored, regenerated by run.py). No longer read by the frontend (see Data flow below) — kept only so `python3 pipeline/run.py` remains useful as a standalone CLI tool without the backend running. No longer read for a "Macro Watchlist" UI either — that feature was removed, and macro_watchlist is dropped entirely from /api/cot/db's response shape

frontend/
  src/App.jsx                Top-level composition and owner of the section-nav tab state (`activeSection`, default `"cot"`). Renders the app title/subtitle ("ArgentVigil" — moved here from silver_cot_tracker.jsx) and the five nav buttons (CoT, Money Supply, Inventory, CATCOR, Data) in one `.app-header--split` row, then all five sections mounted unconditionally with a `section-hidden` (CSS `display:none`) class toggled by `activeSection` — never conditionally rendered/unmounted, so switching tabs doesn't refire each section's mount-time fetches or FORCE_REFRESH_EVENT listeners, and doesn't reset scroll/open-panel state within a section. Section order in the nav matches top-to-bottom composition order (CoT, Money Supply, Inventory, CATCOR, Data).
  src/silver_cot_tracker.jsx CoT panel — reads from /api/cot/db (not the old static cot_data.json). No longer renders the app title/header or a "Configuration" panel (both removed/relocated — see App.jsx above; RefreshControls' UI panel was deleted outright, not moved, though refresh_controls.jsx's FORCE_REFRESH_EVENT export is still used by 4 files, see below). Under "Positioning Extremes / Speculative Crowding" (whose `<summary>` title row now also inline-renders StalenessLabel, right-justified via `margin-left: auto` against a `<span>`-wrapped title — see below): CombinedChart ("CoT Positioning & Gold/Silver Ratio"), then a collapsible "Who's Holding Long Positions" (CategoryCompositionPanel — disaggregated CoT category composition, its own metal toggle, own fetch of /api/delivery-behavior/db; see delivery_behavior.py above) which now defaults **open**, then two collapsible per-metal sections ("Silver", "Gold") which now default **closed** (a deliberate flip from their previous open-by-default state) each containing that metal's PaperLeveragePanel (live spot-price badge from /api/prices/db, leverage from /api/silver|gold/db/leverage), SignalBanner, and SignalTrackRecord. No longer exports MacroWatchlist (removed). `StalenessLabel` no longer takes a `generatedAt` prop or renders the "pipeline run" clause — only "CoT data as of ... · published ~..." remains; `pipeline_runs`/`get_last_run_at()` are still written/read backend-side (unchanged), just no longer surfaced in this label.
  src/comex_inventory.jsx    Exchange Inventory Dashboard, structured as nested collapsible <details> panes: top-level "Stock & Flow" contains CrossExchangePanel, then separate collapsible sections for "COMEX — New York" (vault snapshot, then a sibling "Delivery Behavior" collapsible — see delivery_behavior_panel.jsx below — then nested Registered/Eligible + Delivery Notices sub-panels), "SHFE — Shanghai" (warehouse snapshot + nested SHFE history sub-panel), "Market Demand", "Demand Composition Over Time", and "Global Context" — the latter three are siblings directly under Stock & Flow, not nested inside either exchange. fetchAll() now calls only /db-suffixed routes (never live upstream) — actual upstream refresh happens server-side (see Refresh controls below). `VaultSnapshotPanel` supports being "pinned" to a specific past date (via `pinnedDate`/`pinnedDepositories`/`pinnedLoading` props, driven by hovering a row in `DeliveryBehaviorPanel`'s reclassification table, wired through `ComexInventoryDashboard`'s `handleHoverDate`) — reads `/api/silver/db/depositories?date=...` instead of the latest-only default, so hovering a flagged reclassification date shows that date's real per-vault breakdown if `inventory_depository` has history back that far (it only accumulates going forward from whenever the depositories fetch first ran — no upstream backfill exists for this specific per-vault route, unlike delivery_notices' `ytd` trick below). The panel keeps its previous content mounted (never swaps to a shorter "Loading…"/empty tree) while a pinned-date fetch is in flight, with a `min-height` lock and an absolutely-positioned overlay for the "no snapshot for this date" message — swapping the whole subtree caused Recharts' `ResponsiveContainer` to remount and briefly measure 0×0, which collapsed the pie chart and reflowed the page enough to knock the mouse off the very row being hovered, canceling the pin.
  src/delivery_behavior_panel.jsx  DeliveryBehaviorPanel — reads /api/delivery-behavior/db?metal=. Two panels: "Reclassification vs. Real Inflow" (ReclassificationTable — flagged days where registered inventory rose without matching delivery-notice volume; rows are hoverable when mounted with `onHoverDate` and metal is silver, see comex_inventory.jsx above) and "Short-Term Anomaly vs. Structural Deficit" (annual Silver Institute balance for scale context, reusing MarketBalancePanel's data). Does NOT export the category-composition chart — that moved to silver_cot_tracker.jsx's CategoryCompositionPanel, since it's CoT-derived data (disaggregated, not Legacy) and reads better grouped with AV's other CoT content than under Stock & Flow.
  src/refresh_controls.jsx   RefreshControls component + FORCE_REFRESH_EVENT — reads/writes /api/refresh/settings (two tiers, each independently toggle-able, both OFF by default) and triggers /api/refresh/force. On a successful force-update, dispatches a window CustomEvent (FORCE_REFRESH_EVENT) that comex_inventory.jsx's fetchAll, silver_cot_tracker.jsx's PaperLeveragePanel, and catcor_panel.jsx all listen for, so panels re-read their /db data immediately rather than waiting for their own poll tick. **The `RefreshControls` component itself is no longer rendered anywhere** — its "Configuration" collapsible (formerly in silver_cot_tracker.jsx) was removed at the user's request; the file is kept only because `FORCE_REFRESH_EVENT` is still imported by comex_inventory.jsx, silver_cot_tracker.jsx, catcor_panel.jsx, and delivery_behavior_panel.jsx. The backend refresh-tier settings/force endpoints are unaffected — there's just currently no UI control for them.
  src/market_balance.jsx     Stock & Flow Layer 3 UI — exports MarketBalancePanel (annual balance chart + runway stats) AND DemandCompositionPanel (separate export, since Demand Composition needed to become its own independently-collapsible section rather than being bundled into MarketBalancePanel) — both imported into comex_inventory.jsx
  src/money_supply.jsx       Money Supply panel — M2/WALCL chart, then a "Dollars vs Silver vs Gold as Purchasing Power" chart comparing 4 series (Fiat $100 nominal / Gold XAU / Silver XAG / CPI-derived Purchasing Power) as relative return % against a user-selected baseline (click a legend label to set baseline, checkbox to toggle visibility; PP is not selectable as baseline — "not a holdable asset"). Supports click-and-hold on a chart point to see "since that date to latest" comparison in the tooltip (colored green/red for win/loss vs. baseline), without redrawing the chart lines. Imported directly into App.jsx.
  src/catcor_panel.jsx       CATCOR panel ("Catalyst Correlation") — reads /api/catcor/events/db + /api/catcor/reactions/db. Two Recharts scatter charts sharing a `metal` (XAG/XAU) toggle: "Catalyst Timeline" (top) plots every captured catalyst by real calendar date over a 1M/3M/6M/1Y lookback (fixed 30-day lookahead) — includes events with no known surprise_magnitude yet (can't appear in the chart below) and known-future events rendered at reduced opacity as a placeholder (never a real 0% reaction); "Surprise Magnitude vs. Price Reaction" (below, with its own T-30m/T+5m/T+30m/T+2h window toggle) only plots events where a real surprise_magnitude exists — an event with no consensus yet is never plotted at a misleading x=0. Both charts render diamonds (event-type colored via CATCOR_EVENT_COLORS), and hovering a point in either chart doubles the size of that same event's point in the other (shared hoveredEventId state) — a deliberate cross-chart link since the two charts are two views of the same underlying set of events, not independent. The event-type legend folds in each type's next-scheduled date (no separate "Next Scheduled Catalysts" list).
  src/data_map.js            **Static, hand-authored map of every table AV persists** — one entry per upstream source (CFTC CoT Legacy/Disaggregated, Yahoo weekly/monthly/intraday, metalcharts.org silver/gold/SHFE, Sprott PSLV, spot prices, FRED/ALFRED, CATCOR's seed/ForexFactory/reaction-capture sources), each carrying `origin`, `cadence`, `rateLimit`, a `curl` field (a real, runnable-shape curl reproducing that source's actual upstream HTTP call — verified against the live fetch code, not invented; metalcharts.org's entries document its two-step token flow since a single static curl can't replicate a short-lived bearer token), an optional source-level `note`, and a `tables` array of `[field, description]` pairs per SQLite table. **Not introspected from SQLite at runtime** — provenance/cadence isn't something the DB schema encodes, so this has to be authored by hand and kept in sync manually. Per dataHealth-spec.md (Data Health/Fetch Status v1): each entry also carries `sourceKeys` (the backend `source_health.source_key`(s) this card covers — some cards, e.g. `metalcharts_silver`, cover several) and `healthMeta` (a `{sourceKey: {expectedIntervalS, tier}}` map — `tier` one of `fast`/`slow`/`catcor-event`/`catcor-consensus`/`on-demand`/`pipeline`, `expectedIntervalS` the machine-readable cadence used for client-side stale detection, kept separate from the human-facing `cadence` string). Sources with no periodic fetch of their own (`catcor_calendar`'s static seed, `research`'s on-demand chat) simply omit `sourceKeys`/`healthMeta`.
  src/data_panel.jsx         Renders data_map.js as the "Data" tab's content — `DataPanel` (outer, defaults open) containing a `TieredLoopSummary` (fast/slow tier enabled+interval from the existing `GET /api/refresh/settings`, plus a healthy/total rollup per tier computed from `GET /api/health/db`) above one nested `<details>` per source (`SourceCard`), each showing origin/cadence/rate-limit as key-value rows (reusing `.comex-delivery-item` styling), one `FetchStatusRow` per `sourceKeys` entry (status dot computed client-side as ok/stale/error against that source's `expectedIntervalS`, last-success time, truncated last-error text, and an independent "Re-run now" button hitting `POST /api/health/refresh/{source_key}` — `cot_pipeline`'s button additionally grays out inside its 7-day rate-limit window, per dataHealth-spec.md Decision 3), the `curl` block in a `<pre><code>` (`.data-curl-block` CSS, monospace, horizontally scrollable), then a `.comex-table` per table listing every field and its description. Replaced an earlier `lastFetchEndpoint`/`refreshEndpoint`-based version of `FetchStatusRow` outright (no dual mechanism kept) — that version worked for the `cot` card only, reading `pipeline_runs` directly; the CoT card now reads `source_health.cot_pipeline` the same as every other source, since `pipeline/run.py`'s `run_pipeline_once()` records its own outcome there on every invocation (cron/manual CLI included, not just the in-app button).
  src/palette.js             Shared chart color constants (VAULT_COLORS, CATCOR_EVENT_COLORS)
  vite.config.js             publicDir points at pipeline/cache (legacy — cot_data.json is no longer fetched by the frontend, but the dir is still served in dev/prod); /api proxied to :8000

utils/
  dev.sh              Boots venv, installs deps if missing, kills anything already listening on :8000/:5173 (loudly — prints the offending PID/command before killing, dev environment so speed over caution), then runs FastAPI (:8000) + Vite (:5173) together
  sniff-metal-charts.py, sniffer.sh   Tools for reverse-engineering metalcharts.org API responses
```

**Convention:** runnable scripts live in `utils/`, not the repo root (established preference — keep new launchers/helpers there).

**Where the databases live:**

- `runtime/argentvigil.db` — gitignored, owned by `backend/db.py`. **One shared database for the whole app, and the only file `runtime/` contains** — every exchange-inventory, FRED, and spot-price table; CATCOR's `event_calendar`/`macro_price_reaction`/`spot_price_tick`/`forexfactory_calendar` tables; the CoT pipeline's `cot_silver`/`cot_gold`/`cot_prices`/`pipeline_runs` tables (merged in from a previously-separate `pipeline/cache/cot_pipeline.db` — `pipeline/` no longer owns its own database file; `pipeline/run.py` persists via `from backend import db`, and `backend/main.py`'s `/api/cot/db` route reads the same tables directly via `db.get_silver_series()` etc., no longer through a `cot_db` alias); and Delivery Behavior's `cot_disaggregated` table, also fetched+persisted by `pipeline/run.py` (see Data flow's Delivery Behavior subsection). Path computed `__file__`-relative from `backend/db.py` (two directories up to repo root, then into `runtime/`) — not a bare cwd-relative string, so it resolves correctly regardless of how/where the process is invoked from. ForexFactory's calendar feed used to be cached to a separate flat file (`runtime/forexfactory_thisweek.json`) alongside this database — replaced with the `forexfactory_calendar` table (see CATCOR subsection below) so there's exactly one persistence mechanism in the app, not two.

## Data flow

**Standing rule (persist-on-fetch, see `SPEC.MD`):** every route that talks to an upstream source has a `_fetch_and_persist_*` function that fetches, reshapes, and writes to SQLite; the live route (unprefixed) calls that function and returns its result directly (for cases still needed, e.g. `/api/silver/delivery`), while a paired `/db`-suffixed route reads the same data straight back out of SQLite with no upstream call. The frontend calls **only** `/db`-suffixed (or `/api/cot/db`, `/api/prices/db`) routes — it never triggers a live upstream fetch itself. Something still has to call the upstream side periodically, which is what the tiered background refresh (below) is for.

- **CoT data**: `pipeline/run.py` → parses CFTC rows → persists append-only to `runtime/argentvigil.db` via `from backend import db` (`cot_silver`/`cot_gold` tables, `INSERT OR IGNORE` keyed by `report_date` — a published report is never overwritten) + upserts weekly price history (`cot_prices`, ticker+date) → stamps `pipeline_runs` (single-row table, `id=1`, upserted via `db.record_pipeline_run()`) with the run's completion time → **also** still writes `pipeline/cache/cot_data.json` unchanged, kept only so the pipeline remains useful as a standalone CLI tool without the backend running. The frontend no longer reads that JSON file at all: `silver_cot_tracker.jsx` calls `GET /api/cot/db`, which reads the same `cot_silver`/`cot_gold`/`cot_prices` tables directly via `backend/db.py`'s `get_silver_series`/`get_gold_series`/`get_price_series` (`backend/main.py` calls these as plain `db.*`, not through a separate `cot_db` alias — CoT's persistence was merged into the shared `db.py` module, see "Where the databases live" above) and recomputes percentiles/track-record/GSR server-side on each request (cheap — ~1,560 rows/metal) using `pipeline/compute.py`'s `compute_from_series`/`compute_signal_track_record`. The frontend's staleness banner (`StalenessLabel` in `silver_cot_tracker.jsx`) shows "pipeline run" from `db.get_last_run_at()`, which reads `pipeline_runs` directly — **not** derived from any `cot_silver`/`cot_gold` row's `fetched_at` (an earlier version did that and was wrong: CFTC only publishes a new report ~weekly, so `INSERT OR IGNORE` is a no-op on most runs, meaning the derived value would freeze at whenever the latest report row was first inserted rather than reflecting that the pipeline had genuinely run again). `pipeline_runs` is stamped unconditionally at the end of every successful `main()`, independent of whether any new CoT rows were actually inserted that run. `/api/cot/db` does **not** trigger a live Socrata fetch — CoT refresh stays external (`python3 pipeline/run.py`, manual or cron), a deliberate asymmetry from every other source's `/refresh`+`/db` pattern, since a 15-year Socrata pull is too slow to trigger from an HTTP request; **`POST /api/refresh/force` does not touch CoT or `pipeline_runs` either** — only running the pipeline updates the "pipeline run" timestamp. The CoT panel no longer renders a "Macro Watchlist" section — that manually-edited UI was removed, and `/api/cot/db`'s response drops `macro_watchlist` entirely (not ported to SQLite).
- **Exchange inventory (silver + gold)**: each of COMEX/SHFE/PSLV history, depositories, leverage/OI, delivery notices, and SHFE warehouses has a `_fetch_and_persist_*` function in `main.py` that fetches from metalcharts.org (authenticated via `mc_token.py`) or Sprott, persists to SQLite, and a paired `/db` route reads it back (e.g. `/api/silver/db/depositories`, `/api/silver/db/leverage`, `/api/silver/db/delivery`, `/api/shfe/db/warehouses`, `/api/pslv/db`). Gold has a full parallel route set to silver (`/api/gold/history`, `/api/gold/db/history`, `/api/gold/depositories`, `/api/gold/db/depositories`, `/api/gold/leverage`, `/api/gold/db/leverage`) using `symbol=XAU` and 100oz contracts (vs. silver's `XAG`/5,000oz) — confirmed live against metalcharts.org before building, though only the leverage route currently has a frontend consumer (gold COMEX history/depositories are fetched and persisted every slow-tier cycle but nothing in `comex_inventory.jsx` renders them yet — a known no-op, not a bug). `/api/silver/db/leverage` and `/api/gold/db/leverage` power the CoT panel's per-metal Paper Leverage cards, each also showing a spot price via `/api/prices/db` (backed by a new `spot_price_snapshot` table — kept separate from `fred_observations`' `XAG_CLOSE`/`XAU_CLOSE` daily-close keys since the live spot quote carries a `change_pct_24h` field the generic FRED-style table doesn't have room for).
- **Tiered background refresh** (`main.py`'s `lifespan`): two independent `asyncio` loops keep SQLite warm without the frontend ever calling upstream. **Fast tier** = spot prices only (`/api/prices`'s fetch+persist logic) — the one genuinely intraday-moving figure. **Slow tier** = the other 10 sources (silver/gold COMEX history, depositories ×2, leverage ×2, delivery notices, SHFE history, SHFE warehouses, PSLV) — all move at most daily upstream. Both tiers run once, unconditionally, at server startup (so the app has data immediately even with both toggles off), then default to **disabled** — each tier only repeats on its interval if explicitly turned on. State (interval + enabled flags per tier) is in-memory only, read/written via `GET`/`POST /api/refresh/settings` (body: `{"fast_interval_s", "slow_interval_s", "fast_enabled", "slow_enabled"}`); `POST /api/refresh/force` runs both tiers once immediately regardless of their enabled state and returns real per-tier `{"succeeded", "failed", "errors"}` counts (not a blind `{"success": true}`) so the frontend can tell a genuine failure — e.g. no network — from a real update, and only tells other panels to re-fetch (`refresh_controls.jsx`'s `FORCE_REFRESH_EVENT`, listened for by `comex_inventory.jsx` and `PaperLeveragePanel`) when at least one source actually succeeded. CoT is intentionally excluded from force-update — it stays pipeline-only.
- **Market balance**: `main.py` reads `silver_market_balance.json` directly (no DB, no external fetch) and computes derived metrics (5yr cumulative, runway years, staleness) on each request. Demand Composition Over Time is now a separate exported component (`DemandCompositionPanel` in `market_balance.jsx`) reading the same balance data, so it can be independently collapsible in the UI.
- **Money supply**: frontend calls `/api/fred/money-supply/refresh` (fetches M2SL, WALCL, CPIAUCSL from FRED, upserts SQLite `fred_observations`) and `/api/fred/money-supply/db?window=2y|5y|10y|20y` (reads SQLite, computes YoY + CPI-derived purchasing-power index server-side, returns trillions/percent/index — never raw FRED units). Requires `FRED_API_KEY` in the environment (`.env`, loaded via `python-dotenv`, or shell-exported — either works, see `main.py`'s `load_dotenv()`); `refresh` raises a clean `500` if unset rather than failing silently.
- **Metal price history (for Money Supply's purchasing-power comparison)**: `/api/metals/prices/refresh` fetches DAILY closes from Yahoo Finance (`SI=F`/`GC=F`, httpx directly in `main.py` — not `pipeline/price_fetch.py`, which is a separate stdlib-only weekly-interval helper for the CoT pipeline), resamples to one row per calendar month (last trading day on/before month-end, avoiding Yahoo's `1mo` bucket which includes the current in-progress month), and upserts into the same generic `fred_observations` table under `XAG_CLOSE`/`XAU_CLOSE` series_id keys. `/api/metals/prices/db?window=...` reads that back and returns both raw price and an index-to-100-at-window-start value per point. The frontend then does its own further client-side rebasing against a user-selected baseline (see money_supply.jsx above) — the backend only supplies window-start-indexed values, not baseline-relative ones.
- **CATCOR (event/reaction spine)**: `main.py`'s `lifespan` runs `catcor.seed_events()` (loads `catcor_events_seed.py`'s static FOMC/CPI/NFP list, upserts into `event_calendar` for the seeded 90-day-back/60-day-forward window, `event_id` deterministically `f"{event_type}_{date}"` so re-seeding is idempotent) → `catcor.backfill_intraday_ticks()` + `catcor.backfill_daily_closes()` (Yahoo Finance, see below) → `catcor.fetch_and_persist_consensus()` (ForexFactory) → `catcor.fetch_and_persist_actuals()` (ALFRED) → `catcor.backfill_reactions()`, all best-effort/independently try-excepted so one failure doesn't block the rest. Three tables in `argentvigil.db`: `event_calendar` (event_id PK, includes `source_tier` — currently always `"government"`, see catcor_events_seed.py above), `macro_price_reaction` (PK event_id+metal+window, one row per captured T-30m/T+5m/T+30m/T+2h price reaction), and `spot_price_tick` (PK series_id+ts, **append-only** `INSERT OR IGNORE` — fixes a real gap `spot_price_snapshot` has: that table is one row per calendar day, overwritten on every fast-tier tick, so it has no true intraday series to snapshot from; `spot_price_tick` does, and `main.py`'s `_fetch_and_persist_prices` now writes to both tables on every fast-tier poll).
  - **ALFRED actuals**: `_fetch_alfred_change` fetches two consecutive vintage prints for `CPIAUCSL`/`PAYEMS` as of the day after `scheduled_time` and diffs them — a raw ALFRED level is NOT comparable to ForexFactory's consensus (a period-over-period change), a real bug caught during build. NFP's diff is a raw count (thousands of persons, matching PAYEMS's native unit); CPI's is a % m/m change. `surprise_delta = actual - consensus`, computed whenever both are known (by either fetch running first — order doesn't matter, each recomputes `surprise_delta` if the other value is already present).
  - **Consensus**: `fetch_and_persist_consensus` matches `event_calendar` rows against ForexFactory's free, no-key `ff_calendar_thisweek.json` feed (title + same-instant match, "Non-Farm Employment Change" specifically — not "ADP Non-Farm Employment Change", a different report) by `event_type`. Only a "this calendar week" (Sun-Sat) variant exists — nextweek/lastweek both 404 — so consensus can only ever be captured for events within roughly ±6 days of "now," not retroactively for older backfilled events; this is a permanent, disclosed gap, not a bug. The feed is fetched **at most once per calendar week** and persisted to `forexfactory_calendar` (every entry the feed returns — every country, not just the USD/CPI/NFP subset actually matched — keyed by `(week_key, title, country, event_date)`, `week_key` = that week's Sunday date), checked via `db.has_forexfactory_week()` — confirmed live that repeat hits trip the endpoint's rate limit (429) and a wider community report says it can lock out an IP entirely, so `catcor.py` never re-fetches within the same week regardless of how often `fetch_and_persist_consensus` is called. This used to be a flat-file cache at `runtime/forexfactory_thisweek.json` rather than a SQLite table — moved into the shared database since the file cache was really a second, inconsistent persistence mechanism for real data (the full weekly global economic calendar) that happened to be framed as "just" a rate-limit workaround; only the USD/CPI/NFP subset was ever read back out, but the rest (ECB/BOJ/PBOC decisions, other USD releases like ISM/Unemployment Claims/FOMC speeches) has plausible future value for CATCOR's later iterations and was previously being silently discarded every time the file was overwritten for a new week. A dedicated `_consensus_tier_loop` (`main.py`, 30min interval) calls it periodically — alongside `fetch_and_persist_actuals` (ALFRED shares this loop rather than getting its own, since checking more often than consensus does buys nothing) — so an event that enters the current week after startup is still caught; almost every one of those calls is a pure DB read, not a network hit. ForexFactory's `"K"`-suffixed NFP forecasts (e.g. `"114K"`) must be scaled to PAYEMS's native thousands unit (÷1000, not the naive ×1000) — a second real unit bug caught during build alongside the ALFRED-level-vs-change one above.
  - **Snapshot capture** (`catcor.capture_snapshot`): idempotent by construction — skips entirely if a `macro_price_reaction` row already exists for the given `(event_id, metal, window)`, so repeated calls (a normal restart, or a restart after any length of downtime) are cheap; only genuinely-missing windows do real work. `due_snapshots()` finds `(event_id, window)` pairs whose boundary has passed but aren't fully captured yet — polled every 60s by `main.py`'s `_event_tier_loop` (always-on, not gated by an enabled flag like fast/slow tier, since a missed snapshot window is permanent data loss, unlike fast/slow tier data which is always re-fetchable). Price lookup: `db.get_ticks_near` finds the nearest `spot_price_tick` row within a tolerance window; if none exists, falls back to `catcor.backfill_daily_closes`'s dedicated `XAG_DAILY_CLOSE`/`XAU_DAILY_CLOSE` `fred_observations` keys (NOT the existing `XAG_CLOSE`/`XAU_CLOSE` keys — those are month-end resampled for Money Supply and have no daily granularity at all, a real bug caught during build when the fallback silently returned `NULL` against the live DB). `backfill_intraday_ticks` pulls Yahoo Finance 5-minute bars (`interval=5m`, capped at ~72 days back by Yahoo; 1-minute interval is capped at only 8 days, too short to be useful) — so roughly 72 of a 90-day backfill window get real intraday-precision reactions, and the remaining ~18 oldest days fall back to the coarser same-day daily-close approximation.
  - **Routes**: `GET /api/catcor/events/db` (upcoming events), `GET /api/catcor/reactions/db` (full event×metal×window join for the frontend's two charts), `POST /api/catcor/refresh` (manual re-seed + backfill + re-fetch — returns a clean `500` if `FRED_API_KEY` is unset, same pattern as `fred_money_supply_refresh`).
- **Delivery Behavior**: `GET /api/delivery-behavior/db?metal=XAG|XAU` (no `/refresh` route — everything it reads is either already fetched by another layer's tiered refresh, or fetched by `pipeline/run.py`, not on-demand). Returns `{"reclassification": ..., "category_composition": ..., "deficit_context": ...}`.
  - **Reclassification** (`compute_reclassification_signal`, silver-only): walks `inventory_aggregate`'s day-over-day `registered` deltas, skips any day whose delta spans a `None`-registered gap (can't fairly attribute a multi-day change to one day's delivery volume) or has no matching `delivery_notices` row for that exact date, then flags a day if same-day delivery volume (`daily_issued + daily_stopped`, converted from COMEX contracts to troy oz — 5,000 oz/contract — a real unit-mismatch bug caught during build: comparing contract counts directly against a troy-oz delta made nearly every real delivery day look like a false "reclassification") covers less than 10% of the registered increase. Reads `delivery_notices` with `type="ytd"`, not the `"mtd"` default `_fetch_and_persist_delivery` otherwise uses — confirmed live against metalcharts.org that `ytd` returns the same working `dailyIssued`/`dailyStopped` fields but ~85 days of history vs. `mtd`'s handful of days-in-month, so `main.py`'s slow tier now calls `_fetch_and_persist_delivery(type="ytd")` specifically to give this signal real coverage instead of resetting to near-nothing every month. Each flagged day also carries `total_oz`/`prev_total_oz`/`total_delta` (today vs. yesterday's total vault holdings, not just registered) since total and registered can move in opposite directions on the same day (eligible dropping more than registered rises nets a falling total even on a day registered increased) — the frontend table shows total-vault-yesterday/today/change alongside delivery volume, not registered delta, precisely so this doesn't read as inconsistent against the aggregate chart above it.
  - **Category composition** (`compute_category_composition`, both metals): shares of long open interest by CFTC Disaggregated CoT category (`producer_merchant`/`swap_dealer`/`managed_money`/`other_reportable`) per report_date, plus an approximate `days_to_fnd_approx` per week (see FND rule below) — explicitly documented in the function's own docstring as a directional marker only, NOT a claim about which contract month any category's OI actually sits in, since the Disaggregated report aggregates across every open contract month for the commodity and AV has no contract-month-level OI to disambiguate (see below). Requires all 4 categories present for a report_date before computing shares (skips the week otherwise) rather than silently normalizing over an incomplete total — a code-review finding caught before shipping: the earlier version always sourced `open_interest` from `producer_merchant`'s row specifically and could understate `total_long` if any single category's `INSERT OR IGNORE` failed to land.
  - **First Notice Day / Last Trade Day**: computed on the fly (`first_notice_day`, `last_trade_day`, `last_business_day` in `delivery_behavior.py`), no seed table — confirmed live that both silver and gold list monthly contracts on a rolling basis (silver: 26 consecutive months + Jul/Dec out to 60 months; gold: 26 consecutive months + Jun/Dec out to 72 months, per each metal's CME contract-specs page's "Listed Contracts" line), i.e. effectively every calendar month trades, not the small fixed Mar/May/Jul/Sep/Dec-style cycle `deliveryBehavior-spec.md` originally assumed — so there's no small static list worth seeding, just a pure date rule. Last Trade Day (third-last business day of the delivery month) is confirmed directly against COMEX rulebook Chapters 112 (silver) and 113 (gold), saved to `seed_data/cme/112.pdf`/`113.pdf` for reference — identical rule for both metals. **First Notice Day required a real correction mid-build**: those same rulebook chapters never actually use the term "First Notice Day" (they only describe the delivery/settlement window, first business day of the delivery month through last business day), and an early version of this code incorrectly inferred FND = first business day of the delivery month from that text. A verified real-world example (COMEX's May 2026 silver contract, FND reported as April 30, 2026 — the *last* business day of *April*, i.e. the month *before* delivery) proved that inference wrong; the corrected rule (FND = last business day of the month preceding the delivery month) is what's implemented now. "Business day" throughout is weekday-only (Mon–Fri), deliberately not holiday-aware — acceptable for this signal's approximate-seasonal-marker purpose, not meant as an exact trading-calendar reference.
  - **Permanently out of scope, not deferred**: `contract_month_oi` (per-contract-month open interest/settlement data) and the seasonal-baseline/OI-decay signal and price-lead-lag signal that would depend on it (`deliveryBehavior-spec.md`'s stories #1 and #5). Confirmed live: CME's real per-contract-month OI/settlement data is a paid Market Data Platform product (`cmegroup.com/market-data/market-data-api.html`, tiered pricing from $0.50/GB), not a free scrapeable source like metalcharts.org or ForexFactory turned out to be — this isn't a reverse-engineering gap to revisit, it's a real paywall. CME's site also runs aggressive, inconsistent edge-level (Akamai) bot-blocking independent of the paid-API question — even the free daily-bulletin PDFs (`Section02B_Summary_Volume_And_Open_Interest_Metals_Futures_And_Options.pdf`, which turned out not to have contract-month granularity anyway, only product-level aggregates) and the delivery-notices report intermittently 403 depending on request headers/timing, confirmed both from this environment and from a real browser. `pdfplumber` was pip-installed into `.venv` during this investigation but is unused in any shipped code — safe to leave installed or remove, nothing imports it.

## Conventions

- **Backend**: stdlib `sqlite3` (no ORM), `httpx.AsyncClient` for outbound calls (one shared client via FastAPI lifespan), routes are thin — fetch, reshape, upsert, return `{"success": bool, "data": ...}`. HTTP errors from upstream become `HTTPException(502, str(e))`.
- **Pipeline**: intentionally stdlib-only, no third-party deps — keep it that way (it's meant to run standalone without the venv). Config constants belong in `pipeline/config.py`, not scattered inline.
- **Frontend**: React 19 + Vite 5 + Recharts, no state library, no router (single page, tab-like sections via `App.jsx`). Chart colors come from `palette.js`, not ad hoc hex values.
- **Units**: COMEX figures are troy oz; SHFE is kg natively, converted via `1 kg = 32.1507 oz` (constant appears in both `main.py` and any frontend display — keep consistent). COMEX open interest contracts are 5,000 oz each.
- **Nulls over zeros**: metalcharts.org sometimes reports `0` to mean "not reported that day" — `_parse_aggregate_row` in `main.py` converts those to `None` so charts gap instead of showing false dips. Apply the same treatment to any new fields with this failure mode.
- **AV Voice Rules** (from SPEC.MD, applies repo-wide to any user-facing copy): no "alarming"/"critical" framing on deficit trends, always show the ±20% uncertainty qualifier on above-ground stock figures, label runway/track-record figures explicitly as historical description or context — never as prediction.
- **Collapsible sections**: the established UI pattern for any collapsible pane is native `<details className="collapsible-pane"><summary className="collapsible-pane-title">…</summary><div className="collapsible-pane-body">…</div></details>` (CSS in `index.css`) — nest freely (e.g. COMEX/SHFE sections each contain their own nested sub-collapsibles). Prefer this over a `useState` show/hide toggle for new collapsible UI unless more complex interaction state is needed.
- **Custom legends over Recharts' built-in `<Legend>`**: chart legends in this codebase are hand-rolled (`.comex-legend-list`/`.comex-legend-item`/`.comex-legend-swatch` classes), one entry per line with an inline plain-English definition — not Recharts' default legend component. Some legend rows are also clickable (e.g. toggle a line's visibility, or select it as a baseline) via a wrapping `<button className="comex-legend-item legend-btn-row">`.
- **Custom scatter-point shapes for cross-chart consistency**: CATCOR's two charts and its legend all use the same diamond shape per event type (`.comex-legend-swatch--diamond` for the legend; a shared `makeLinkedShape` renderer for the charts themselves) so an event type reads identically everywhere rather than circles in one chart and a line-swatch in the legend.
- **The Data tab (`frontend/src/data_map.js` + `data_panel.jsx`) MUST be updated whenever app data changes — this is a strict requirement, not a nice-to-have.** Any change that adds/removes/renames a SQLite table or column (`backend/db.py`'s DDL), adds/removes/changes an upstream source or its fetch shape (new route in `main.py`/`catcor.py`, new pipeline fetch in `pipeline/fetch.py`/`price_fetch.py`), or changes a source's fetch cadence/rate-limit posture (tiered refresh settings, CATCOR's polling intervals) must land a matching edit to `data_map.js` in the same change — new/updated `origin`/`cadence`/`rateLimit`/`curl`/`tables` entries, or removal of a retired source/table. This page is meant to be the **single source of truth for what data AV has, where it came from, and how it's fetched** — treat a data-shape change without a Data-tab update as incomplete, the same as a migration without a schema update.

## Feature pipeline state (per SPEC.MD)

Stock & Flow panel status — **all four layers implemented**, all rendered inside
`ComexInventoryDashboard` (`comex_inventory.jsx`):

- ✅ Layer 1 (static above-ground stock snapshot) — `GlobalSilverPanel` in `comex_inventory.jsx`; category bars + survey-year header + staleness flag (>18mo) + explicit note on the ETF/exchange-vault tracking gap (AV's live COMEX+SHFE+PSLV is a subset of the Silver Institute's broader "ETF & Exchange Vaults" figure)
- ✅ Layer 2 (live exchange inventory: COMEX/SHFE/PSLV) — implemented, in production
- ✅ Layer 3 (`silver_market_balance.json` + `/api/silver/market-balance` endpoint + `market_balance.jsx` UI, rendered via `MarketBalancePanel` inside `ComexInventoryDashboard`) — implemented end-to-end
- ✅ Layer 4 (personal stack calculator) — implemented, inside `GlobalSilverPanel`
- ✅ Cross-panel dynamic linkage — `GlobalSilverPanel` computes the live tracked total (COMEX+SHFE+PSLV) against the static Layer 1 above-ground total on every render, no hardcoded ratio
- 📌 Known data gap: annual market balance series is missing 2005–2013 (Silver Institute WSS PDFs located but not parsed) — documented as a follow-up, not a blocker
- 📌 Known upstream data quality issue: metalcharts.org's COMEX delivery-notices response always returns `mtdCumulative`/`ytdCumulative` as `0` (confirmed across Feb–Jul, never populated) — these two fields are dropped from the Delivery Notices table in `comex_inventory.jsx` rather than displayed as misleading zeros. `dailyIssued`/`dailyStopped` are the real, working fields.

CATCOR Iteration 1 (event/reaction spine) — **implemented end-to-end**, rendered as `CatcorPanel` ("Catalyst Correlation"):

- ✅ Event calendar, ALFRED actuals, ForexFactory consensus, Yahoo intraday/daily-close backfill, idempotent snapshot capture — all implemented and verified live against real data (see Data flow's CATCOR subsection above for the full mechanism)
- ✅ Two-chart frontend (catalyst timeline + surprise-magnitude scatter) with cross-chart hover linking
- 📌 Known gap: `consensus_value` can only ever be populated for events within the current Sun-Sat calendar week (ForexFactory's feed has no historical/future window) — events outside that window keep `actual_value` from ALFRED but never get a `surprise_magnitude`, a permanent limitation of the free consensus source, not a bug to chase
- 📌 Known gap: intraday-precision price reactions only exist for the ~72 most recent days of the 90-day backfill window (Yahoo Finance's 5-minute-interval cap) — older events fall back to a same-day daily-close approximation
- 📌 Not yet built: Iterations 2+ (chat-based claim extraction, FedWatch, standing watchlists, sentiment scoring — see `SPEC.MD`)

Delivery Behavior (per `deliveryBehavior-spec.md`'s six stories) — **stories #3, #6, #2 implemented**; #1 and #5 permanently out of scope, #4 not started:

- ✅ Story #3 (reclassification vs. real inflow) — `compute_reclassification_signal`, silver-only, no new upstream source (pure computation over `inventory_aggregate` + `delivery_notices`)
- ✅ Story #6 (squeeze vs. deficit separator) — `compute_deficit_context`, repackages the existing Silver Institute annual balance series alongside the short-term reclassification window
- ✅ Story #2 (who's standing for delivery) — `compute_category_composition`, both metals, backed by a new Disaggregated CoT fetch (`cot_disaggregated` table, CFTC dataset `72hh-3qpy`) — but only the composition-over-time half is built; the FND-relative "whose OI is persisting into delivery" cross-reference is approximate only (`days_to_fnd_approx`), not precise, since that needs contract-month-level OI (see below)
- 🚫 Story #1 (seasonal-baseline OI-decay) and story #5 (price lead/lag on a delivery-behavior anomaly) — **permanently out of scope, not deferred**: both need `contract_month_oi` (real per-contract-month open interest), and CME's genuine source for that is a paid Market Data Platform product, not a free scrapeable page — confirmed live, see Data flow's Delivery Behavior subsection for the full investigation
- 📌 Not yet built: story #4 (CME firm-level delivery concentration) — the free `MetalsIssuesAndStopsReport.pdf` exists and is fetchable (confirmed live via `httpx` with browser-like headers), but CME's edge (Akamai) bot-blocking is aggressive and inconsistent enough that even that free PDF 403'd unpredictably during this investigation; would need the same kind of reverse-engineering effort `mc_token.py` needed for metalcharts.org, not yet attempted
- 📌 Known gap: `VaultSnapshotPanel`'s hover-to-pin-a-past-date feature (see `comex_inventory.jsx` above) only has real per-depository history from whenever `inventory_depository` first started being populated (no upstream backfill exists for that specific metalcharts.org route, unlike `delivery_notices`) — hovering an older flagged reclassification date shows a "no snapshot persisted for this date yet" message rather than real data, and that coverage window only grows going forward

## Running it

```bash
bash utils/dev.sh          # everything (venv bootstrap + backend :8000 + frontend :5173)
python3 pipeline/run.py    # CoT pipeline only, no server needed
```

Run the pipeline at least once before the frontend, since the CoT tab reads from `/api/cot/db`, which reads `cot_silver`/`cot_gold` from `runtime/argentvigil.db` — empty tables make that route return a `500` ("No CoT data persisted yet. Run pipeline/run.py first.").

The exchange-inventory/FRED/spot-price data populates itself on first backend startup regardless (a one-time unconditional refresh runs in `main.py`'s `lifespan`) — the tiered background refresh only *repeats* on a schedule if explicitly enabled via the "Configuration" panel in the UI (both tiers default off) or via `POST /api/refresh/force`. CATCOR's event calendar and reaction backfill also run automatically on every startup (`catcor.seed_events()` etc., see Data flow's CATCOR subsection) — no manual trigger needed, though `/api/catcor/refresh` exists for an on-demand re-run. ALFRED calls need `FRED_API_KEY` set in whatever shell launches the backend (same requirement as Money Supply, above) — without it, the event calendar and price reactions still populate, but `actual_value`/`surprise_delta` stay `NULL`.

**Always run Python through `.venv`, never bare `python3`.** `bash utils/dev.sh` creates
`.venv` on first run and installs `requirements.txt` into it (`fastapi`, `uvicorn`, `httpx`,
`python-dotenv` — none of these are on system Python). Before running any Python command
against this repo — import checks, one-off scripts, `python -c "..."` sanity tests — run
`source .venv/bin/activate` first, or invoke `.venv/bin/python` directly. `pipeline/` is the one
exception: it's intentionally stdlib-only by design (see Conventions above) and *can* run under
bare `python3`, but `main.py`/`db.py`/anything importing `fastapi`, `httpx`, or `dotenv` cannot.

## Data sources (see README.md for full table)

CFTC PRE Socrata API (CoT — both Legacy `jun7-fc8e` and Disaggregated `72hh-3qpy` datasets), Yahoo Finance (SLV/GLD/GC=F/SI=F), metalcharts.org (COMEX/SHFE inventory, volume/OI, delivery notices, spot prices — reverse-engineered API, auth via `mc_token.py`), Sprott direct API (PSLV), Silver Institute World Silver Survey (manually transcribed, annual), ALFRED (FRED's point-in-time vintage API — CATCOR's `actual_value`s), ForexFactory (`nfs.faireconomy.media`'s free calendar export — CATCOR's `consensus_value`s, current-calendar-week only), CME Group COMEX rulebook (Chapters 112/113 — Delivery Behavior's Last Trade Day rule, `seed_data/cme/`). CME's Market Data Platform (per-contract-month OI/settlement, the `get_settlements` API) was investigated and confirmed **paid**, not a free source — not integrated, see Feature pipeline state's Delivery Behavior section.
